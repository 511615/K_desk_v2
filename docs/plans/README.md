# Implementation plans

Plans in this directory describe approved future work. They are not current-state contracts and do
not enter `feature-registry.json`. Each plan must state what is not implemented, name its governing
Feature ID and define the acceptance gate required before the feature document can be updated.
