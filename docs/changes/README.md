# Change records

Create one immutable Markdown file under `unreleased/` for every feature addition, modification,
bug fix, deletion, refactor, UI interaction or operational change. Use `_template.md`; reference at
least one existing Feature ID. Release automation summarizes records into `CHANGELOG.md` but keeps
the original records for audit.
