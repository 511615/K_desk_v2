from __future__ import annotations


def render_kuzu_risk_page() -> str:
    return """<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Kuzu 关联风险扩散</title><style>
:root{font-family:"Microsoft YaHei",Arial,sans-serif;color:#e5e7eb;background:#0b1220}*{box-sizing:border-box}body{margin:0}.bar{padding:14px 22px;border-bottom:1px solid #263246;background:#111827;display:flex;gap:14px;align-items:center}.back{color:#93c5fd;text-decoration:none;border:1px solid #2563eb;border-radius:4px;padding:5px 9px}.brand{font-size:18px;font-weight:700}.hint{font-size:12px;color:#aeb7c2}.stage{padding:18px;max-width:1500px;margin:auto}.tools{display:flex;gap:12px;align-items:center;margin-bottom:12px;flex-wrap:wrap}.tools input{width:72px;background:#111827;color:#fff;border:1px solid #475569;border-radius:4px;padding:7px}.tools input[type=checkbox]{width:auto;padding:0;accent-color:#2563eb}.tools button{background:#2563eb;color:#fff;border:0;border-radius:4px;padding:8px 12px;cursor:pointer}.legend{display:flex;gap:10px;font-size:12px;color:#cbd5e1;flex-wrap:wrap}.dot{display:inline-block;width:10px;height:10px;border-radius:50%;margin-right:4px}.board{height:390px;border:1px solid #334155;border-radius:8px;background:#101826;overflow:hidden}.board canvas{width:100%;height:100%;display:block;touch-action:none;cursor:grab}.board canvas:active{cursor:grabbing}.overview-note{padding:7px 1px 0;color:#93acc6;font-size:12px}.group-legend{display:flex;gap:10px;flex-wrap:wrap;padding:3px 0 9px;color:#9fb6cf;font-size:11px}.group-legend b{color:#d9e8f8}.caption{padding:10px 0 16px;color:#b8cce1;font-size:13px}.caption b{color:#fff}.caption span{color:#85d3ff}.detail{border-top:1px solid #334155;padding-top:15px}.detail h2{font-size:16px;margin:0 0 12px}.flow{display:grid;grid-template-columns:135px 205px minmax(0,1fr);gap:26px;align-items:center}.subject{text-align:center;position:relative}.subject:after{content:"";position:absolute;top:50%;left:100%;width:26px;border-top:2px solid #58c4ff}.subject b{display:flex;align-items:center;justify-content:center;width:92px;height:92px;border-radius:50%;margin:auto;background:#ff2638;border:3px solid #ffd6dc;font-size:18px;box-shadow:0 0 0 7px rgba(255,38,56,.14)}.subject small{display:block;color:#a8bfd7;font-size:12px;margin-top:8px}.groups{display:grid;gap:8px}.group{font:inherit;text-align:left;color:#dceaf9;background:#10233c;border:1px solid #365879;border-left:6px solid #6d87a7;border-radius:7px;padding:9px 10px;cursor:pointer}.group strong{display:block;font-size:13px}.group span{display:block;color:#9fb6cf;font-size:11px;margin-top:3px}.group.active{border-left-color:#58c4ff;background:#183959;outline:1px solid #58c4ff}.cluster{border:1px solid #355573;border-radius:8px;background:#0e1c2f;padding:11px 13px}.cluster header{display:flex;justify-content:space-between;gap:8px;font-size:14px}.cluster header span{font-size:12px;color:#9fb9d5}.people{display:flex;flex-wrap:wrap;gap:7px;margin-top:9px}.person{min-width:78px;padding:7px 9px;border-radius:5px;background:#602d38;border:1px solid #e76b7d;text-align:center;font-size:12px}.person.mid{background:#654126;border-color:#eaa05a}.person.stop{background:#164a3e;border-color:#3ec589}.person strong{display:block}.person small{display:block;color:#d7e4f2;font-size:11px;margin-top:2px}.empty{color:#9fb6cf;font-size:13px}@media(max-width:700px){.bar{padding:12px}.stage{padding:14px}.board{height:340px!important}.flow{grid-template-columns:1fr;gap:13px}.subject{display:flex;align-items:center;gap:13px;text-align:left}.subject b{margin:0;flex:0 0 auto}.subject:after{display:none}.groups{grid-template-columns:repeat(2,minmax(0,1fr))}}</style><style>.overview-layout{display:grid;grid-template-columns:250px minmax(0,1fr);gap:13px;align-items:stretch}.risk-table{border:1px solid #355573;border-radius:8px;background:#0e1c2f;min-height:0;overflow:hidden;display:flex;flex-direction:column}.risk-table header{padding:12px 13px 9px;border-bottom:1px solid #29435f}.risk-table h2{font-size:15px;margin:0;color:#f8fafc}.risk-table header span{display:block;margin-top:4px;color:#9fb6cf;font-size:11px}.risk-table-body{overflow:auto;max-height:620px;padding:6px}.risk-row{width:100%;font:inherit;text-align:left;color:#e5edf7;background:transparent;border:1px solid transparent;border-radius:6px;padding:8px;cursor:pointer;display:grid;grid-template-columns:minmax(0,1fr) auto;gap:5px}.risk-row:hover,.risk-row.active{background:#183959;border-color:#58c4ff}.risk-row strong{font-size:14px}.risk-row small{grid-column:1;color:#9fb6cf;font-size:11px}.risk-status{align-self:start;font-size:12px;font-weight:700;border-radius:4px;padding:2px 6px}.risk-status-T{color:#fde68a;background:#78350f}.risk-status-TA{color:#fecdd3;background:#881337}.risk-status-A{color:#fee2e2;background:#7f1d1d}.risk-empty{padding:13px;color:#9fb6cf;font-size:12px;line-height:1.6}@media(max-width:900px){.overview-layout{grid-template-columns:1fr}.risk-table-body{max-height:175px}.risk-table{order:2}}.copy-inspector{position:fixed;inset:0;z-index:20;display:none;align-items:center;justify-content:center;padding:24px;background:rgba(2,6,23,.76)}.copy-inspector.open{display:flex}.copy-inspector-dialog{width:min(1040px,96vw);max-height:min(780px,92vh);overflow:hidden;display:flex;flex-direction:column;background:#0e1c2f;border:1px solid #64748b;border-radius:8px;box-shadow:0 18px 55px rgba(0,0,0,.48)}.copy-inspector-header{display:flex;align-items:flex-start;justify-content:space-between;gap:14px;padding:16px 18px 12px;border-bottom:1px solid #29435f}.copy-inspector-header h2{margin:0;font-size:17px}.copy-inspector-header p{margin:5px 0 0;color:#9fb6cf;font-size:12px}.copy-inspector-close{background:transparent!important;color:#cbd5e1!important;border:1px solid #475569!important;padding:4px 8px!important;font-size:18px}.copy-inspector-tabs{display:flex;gap:8px;padding:10px 18px 0}.copy-inspector-tabs button{background:#10233c;color:#b8cce1;border:1px solid #365879;border-radius:5px 5px 0 0;padding:8px 11px}.copy-inspector-tabs button.active{background:#183959;color:#fff;border-color:#58c4ff}.copy-inspector-content{overflow:auto;padding:14px 18px 20px}.copy-inspector-summary{display:flex;gap:9px;flex-wrap:wrap;margin-bottom:12px;color:#cbd5e1;font-size:12px}.copy-inspector-summary span{padding:5px 8px;background:#10233c;border:1px solid #29435f;border-radius:4px}.copy-inspector-table{width:100%;border-collapse:collapse;font-size:12px}.copy-inspector-table th,.copy-inspector-table td{padding:8px;border-bottom:1px solid #263b55;text-align:left;vertical-align:top}.copy-inspector-table th{position:sticky;top:0;background:#10233c;color:#9fb6cf}.copy-inspector-table td{color:#dceaf9}.copy-inspector-muted{color:#9fb6cf;font-size:13px;padding:18px 0}.copy-inspector-error{color:#fda4af;font-size:13px;padding:18px 0}@media(max-width:700px){.copy-inspector{padding:10px}.copy-inspector-dialog{width:100%;max-height:96vh}.copy-inspector-header,.copy-inspector-content{padding-left:12px;padding-right:12px}.copy-inspector-tabs{padding-left:12px}.copy-inspector-table{font-size:11px}}</style></head><body>
<header class="bar"><a class="back" id="back" href="/">← 返回账户</a><span class="brand">Kuzu 关联风险扩散</span><span class="hint">上方按账户扩散深度定位；下方查看关系证据</span><span class="hint" id="status">读取中…</span></header>
<main class="stage"><div class="tools"><label>扩散阈值 <input id="threshold" type="number" min="1" max="100" value="20"></label><label title="全平台同步开平仓核验会明显增加耗时"><input id="includeToxic" type="checkbox"> 包含 Toxic 同向/反向开平仓时间匹配（较慢）</label><button id="reload">重新计算</button><span class="legend"><span><i class="dot" style="background:#ff2638"></i>问题账户 / 100 分</span><span><i class="dot" style="background:#ff6976"></i>高分</span><span><i class="dot" style="background:#f5a64b"></i>低分</span><span><i class="dot" style="background:#35c285"></i>阈值停止</span><span><i class="dot" style="background:#0f766e"></i>「叶」已核查，无新增账户</span><span><i class="dot" style="background:#58c4ff"></i>当前账户路径</span></span></div><div class="board"><canvas id="overview" aria-label="按扩散深度定位的全局关系图"></canvas></div><div class="overview-note" id="overviewNote">全部发现节点均单独布局；滚轮最低缩放 10%，点击节点会高亮同组成员。</div><div class="group-legend"><b>点形：</b><span>● 交易账户</span><span>⬡ IB 身份</span><span>◆ 阈值停止</span><b>色块关系：</b><span><i class="dot" style="background:#60a5fa"></i>同 CRM</span><span><i class="dot" style="background:#a78bfa"></i>LastIP</span><span><i class="dot" style="background:#22d3ee"></i>EA</span><span><i class="dot" style="background:#f472b6"></i>跟单</span><span><i class="dot" style="background:#fbbf24"></i>返佣</span><span><i class="dot" style="background:#818cf8"></i>IB</span><span><i class="dot" style="background:#2dd4bf"></i>同名</span><span><i class="dot" style="background:#fb7185"></i>Toxic</span></div><div class="caption"><b id="location">当前查看：-</b> <span id="path">关系路径说明：选择上方账户查看它如何与问题账户相连。</span></div><section class="detail"><h2>当前账户细查</h2><div class="flow"><div class="subject"><b id="selected">-</b><small id="selectedNote">-</small></div><div class="groups" id="groups"></div><article class="cluster"><header><b id="evidence">-</b><span id="evidenceNote">-</span></header><div class="people" id="people"></div></article></div></section></main>
<section class="copy-inspector" id="copyInspector" aria-modal="true" aria-label="跟单订单明细" role="dialog"><div class="copy-inspector-dialog"><header class="copy-inspector-header"><div><h2 id="copyInspectorTitle">跟单订单明细</h2><p id="copyInspectorNote">读取中…</p></div><button class="copy-inspector-close" id="copyInspectorClose" type="button" aria-label="关闭">×</button></header><nav class="copy-inspector-tabs" aria-label="跟单明细选项"><button id="copyEdgeTab" class="active" type="button">本线条订单关系</button><button id="copyMasterTab" type="button">该单主的全部跟单账户</button></nav><div class="copy-inspector-content" id="copyInspectorContent"></div></div></section>
<!-- 色块固定表示关系；白色虚线：当前选中（不改色） --><script>
// The galaxy canvas has accumulated several compatibility click listeners over
// time.  This first capture listener is the single authoritative dispatcher: it
// consumes every click and works from the last rendered, immutable hit frame.
// A click must never call ringLayout(), because layout mutates node positions.
let galaxyHitFrame={markers:[],nodes:[],groups:[],edges:[]};
function galaxyScreenDistanceToSegment(point,start,end){const dx=end.x-start.x,dy=end.y-start.y,length=dx*dx+dy*dy;if(!length)return Math.hypot(point.x-start.x,point.y-start.y);const ratio=Math.max(0,Math.min(1,((point.x-start.x)*dx+(point.y-start.y)*dy)/length));return Math.hypot(point.x-(start.x+ratio*dx),point.y-(start.y+ratio*dy))}
function galaxyScreenDistanceToPath(point,path){let best=Infinity;for(let index=1;index<path.length;index++)best=Math.min(best,galaxyScreenDistanceToSegment(point,path[index-1],path[index]));return best}
function galaxyHitAngleBetween(angle,start,end){const norm=value=>(value+Math.PI*2)%(Math.PI*2),a=norm(angle),s=norm(start),e=norm(end);return s<=e?a>=s&&a<=e:a>=s||a<=e}
function galaxyPickHit(mouse,frame=galaxyHitFrame){
  for(const hit of frame.markers)if(Math.hypot(mouse.x-hit.x,mouse.y-hit.y)<=hit.radius)return{kind:'marker',target:hit.group};
  for(const hit of frame.nodes)if(Math.hypot(mouse.x-hit.x,mouse.y-hit.y)<=hit.radius)return{kind:'node',target:hit.node,groupKey:hit.groupKey||''};
  for(const hit of frame.groups){if(hit.anchor&&Math.hypot(mouse.x-hit.anchor.x,mouse.y-hit.anchor.y)<=hit.anchor.radius)return{kind:'group',target:hit.group};const dx=mouse.x-hit.center.x,dy=mouse.y-hit.center.y;if(Math.abs(Math.hypot(dx,dy)-hit.radius)<=hit.tolerance&&galaxyHitAngleBetween(Math.atan2(dy,dx),hit.start,hit.end))return{kind:'group',target:hit.group}}
  for(const hit of frame.edges)if(galaxyScreenDistanceToPath(mouse,hit.path)<=hit.tolerance)return{kind:'edge',target:hit.edge};
  return{kind:'empty',target:null};
}
function galaxyDispatchClick(event){
  event.preventDefault();event.stopImmediatePropagation();
  if(!data||copyInspector?.classList.contains('open'))return;
  const rect=galaxyCanvas.getBoundingClientRect(),mouse={x:event.clientX-rect.left,y:event.clientY-rect.top},hit=galaxyPickHit(mouse);
  if(hit.kind==='marker'){expandedRelationGroups.delete(hit.target.key);activeGroupKey=hit.target.key;selectedEdgeKey='';renderOverview();renderDetail();return}
  if(hit.kind==='node'){selectedId=hit.target.id;activeGroupKey=hit.groupKey;activeType='';selectedEdgeKey='';selectedEdgeNodes=new Set();renderOverview();renderDetail();return}
  if(hit.kind==='group'){expandedRelationGroups.add(hit.target.key);activeGroupKey=hit.target.key;selectedEdgeKey='';renderOverview();renderDetail();return}
  if(hit.kind==='edge'){selectedEdgeKey=hit.target.id;activeGroupKey=edgeCommunityKey(hit.target);renderOverview();renderDetail();if(relationKey(hit.target.type)==='copy_order')fetchCopyInspection(hit.target)}
}
function galaxyRebuildHitFrame(){
  if(!data){galaxyHitFrame={markers:[],nodes:[],groups:[],edges:[]};return}
  const scale=Math.max(view.scale||1,.01),toScreen=point=>({x:view.x+point.x*scale,y:view.y+point.y*scale}),collapsed=typeof collapsedVisualGroups!=='undefined'?(collapsedVisualGroups||[]):[],collapsedKeys=new Set(collapsed.map(group=>group.key)),markers=[],nodes=[],groups=[],edges=[];
  for(const group of typeof expandedVisibleBands!=='undefined'?(expandedVisibleBands||[]):[]){const point=galaxyExpandedMarkerPoint(group),screen=toScreen(point);markers.push({x:screen.x,y:screen.y,radius:Math.max(18,13*scale+8),group})}
  for(const node of graphNodes()){const point=layout.get(node.id);if(!point||collapsedKeys.has(point.groupKey))continue;const screen=toScreen(point);nodes.push({x:screen.x,y:screen.y,radius:nodeHitPixels(node,scale),node,groupKey:point.groupKey||''})}
  const centerPoint=expandedBandCenter||collapsed[0]?.center;
  if(centerPoint){const center=toScreen(centerPoint);for(const group of collapsed){const anchor=group.anchor?toScreen(group.anchor):null;groups.push({group,center,radius:Number(group.radius||0)*scale,start:group.start,end:group.end,tolerance:Math.max(18,22*scale),anchor:anchor?{...anchor,radius:Math.max(22,18*scale+8)}:null})}}
  for(const edge of typeof relationHitEdges!=='undefined'?(relationHitEdges||[]):[]){const route=relationRoute(edge);if(!route)continue;const path=[];for(let index=0;index<=20;index++)path.push(toScreen(quadraticPoint(route,index/20)));edges.push({edge,path,tolerance:Math.max(10,12*scale)})}
  galaxyHitFrame={markers,nodes,groups,edges};
}
const galaxyCanvas=document.getElementById('overview');
galaxyCanvas.addEventListener('click',galaxyDispatchClick,true);
const ungroupedSelectedBranch=selectedBranch;
const rangeStart=document.createElement('input');rangeStart.type='datetime-local';rangeStart.id='rangeStart';const rangeEnd=document.createElement('input');rangeEnd.type='datetime-local';rangeEnd.id='rangeEnd';const rangeHint=document.createElement('span');rangeHint.className='hint';rangeHint.textContent='留空=全历史';const rangeStartLabel=document.createElement('label');rangeStartLabel.textContent='开始时间 ';rangeStartLabel.append(rangeStart);const rangeEndLabel=document.createElement('label');rangeEndLabel.textContent='结束时间 ';rangeEndLabel.append(rangeEnd);const toolsBar=document.querySelector('.tools'),toxicLabel=document.getElementById('includeToxic')?.parentElement;if(toxicLabel){toolsBar.insertBefore(rangeStartLabel,toxicLabel);toolsBar.insertBefore(rangeEndLabel,toxicLabel);toolsBar.insertBefore(rangeHint,toxicLabel)}
const canvas=document.getElementById('overview'),ctx=canvas.getContext('2d'),threshold=document.getElementById('threshold'),status=document.getElementById('status'),groups=document.getElementById('groups'),people=document.getElementById('people');let data=null,selectedId='',activeType='',activeGroupKey='',view={scale:1,x:0,y:0},drag=null,layout=new Map();const radar=document.createElement('div');radar.setAttribute('aria-label','关系扩散扫描中');Object.assign(radar.style,{display:'none',position:'absolute',inset:'0',pointerEvents:'none',overflow:'hidden'});canvas.parentElement.style.position='relative';radar.innerHTML='<svg viewBox="0 0 1 1" width="100%" height="100%" aria-hidden="true"><g id="radarSweep" transform="translate(.5 .5)"><path id="radarFan" d="M0 0 L0 -1 A1 1 0 0 1 .31 -.95 Z" fill="rgba(70,210,255,.18)"><animateTransform attributeName="transform" type="rotate" from="0" to="360" dur="1.8s" repeatCount="indefinite"/></path><line id="radarBeam" x1="0" y1="0" x2="0" y2="-1" stroke="rgba(100,225,255,.9)" stroke-width="2.4"><animateTransform attributeName="transform" type="rotate" from="0" to="360" dur="1.8s" repeatCount="indefinite"/></line></g></svg>';canvas.parentElement.append(radar);const radarSvg=radar.querySelector('svg'),radarSweep=radar.querySelector('#radarSweep'),radarFan=radar.querySelector('#radarFan'),radarBeam=radar.querySelector('#radarBeam');function positionRadar(){const subject=accounts().find(node=>node.isSubject),point=subject&&layout.get(subject.id),bounds=canvas.getBoundingClientRect();if(!point||!bounds.width||!bounds.height)return;const radius=Math.hypot(bounds.width,bounds.height),angle=Math.PI/10,endX=Math.sin(angle)*radius,endY=-Math.cos(angle)*radius,x=point.x*view.scale+view.x,y=point.y*view.scale+view.y;radarSvg.setAttribute('viewBox','0 0 '+bounds.width+' '+bounds.height);radarFan.setAttribute('d','M0 0 L0 -'+radius+' A '+radius+' '+radius+' 0 0 1 '+endX+' '+endY+' Z');radarBeam.setAttribute('y2',String(-radius));radarSweep.setAttribute('transform','translate('+x+' '+y+')')}new MutationObserver(()=>{radar.style.display=status.textContent.includes('后台扩散中')?'block':'none'}).observe(status,{childList:true,characterData:true,subtree:true});
const relationshipLegend=document.querySelector('.group-legend');if(relationshipLegend){const selectedLegend=document.createElement('span');selectedLegend.textContent='白色虚线：当前选中（不改色）';relationshipLegend.append(selectedLegend)}function relationMap(){return new Map((data.relationTypes||[]).map(x=>[x.id,x.label]))}function accounts(){return data.entities.filter(x=>x.type==='account')}function graphNodes(){const nodes=data.entities.filter(x=>x.type==='account'||x.type==='ib_user'),subject=nodes.find(node=>node.isSubject);if(!subject)return nodes;return nodes.filter(node=>node.isSubject||route(node)[0]?.id===subject.id)}function byId(){return new Map(data.entities.map(x=>[x.id,x]))}function primary(node){return (node.scoreLedger||[])[0]||null}function directSubjectEdge(node){const subject=accounts().find(item=>item.isSubject);if(!subject||node.isSubject)return null;return data.relationships.find(edge=>(edge.source===subject.id&&edge.target===node.id)||(edge.target===subject.id&&edge.source===node.id))||null}function parent(node){const direct=directSubjectEdge(node);if(direct)return accounts().find(item=>item.isSubject)||null;let current=node,seen=new Set(),map=byId();while(current&&!seen.has(current.id)){seen.add(current.id);const ledger=primary(current);if(!ledger)return null;const from=map.get(ledger.from);if(!from)return null;if(from.type==='account'||from.type==='ib_user')return from;current=from}return null}
function accountDepth(node){if(node.isSubject)return 0;let depth=0,current=node,seen=new Set();while(current&&!current.isSubject&&!seen.has(current.id)){seen.add(current.id);const next=parent(current);if(!next)return Math.max(1,Math.ceil(Number(node.hops||1)/2));depth++;current=next}return Math.max(1,depth)}function terminalState(node){if(!node||node.type!=='account'||node.isSubject||data.inProgress||data.truncated)return'';if(!node.expandable)return'threshold';return accounts().some(item=>item.id!==node.id&&parent(item)?.id===node.id)?'':'leaf'}function checkedLeaf(node){return terminalState(node)==='leaf'}function branchType(node){const direct=directSubjectEdge(node);if(direct)return direct.type;const ledger=primary(node);return ledger?ledger.type:'unknown'}function route(node){const result=[];let current=node,seen=new Set();while(current&&!seen.has(current.id)){result.unshift(current);seen.add(current.id);current=parent(current)}return result}function isDirectedRelation(type){return new Set(['crm_owner','direct_ib','ib_owned_account','ib_direct_account','ib_identity','ib_direct_rebate']).has(type)}function relationshipCluster(node){const map=byId(),type=branchType(node),seed=map.get(node.id),walked=new Set(seed?[seed.id]:[]),queue=seed?[seed]:[],actual=[],actualSeen=new Set();if(!seed)return{ids:new Set(),edges:[],type};while(queue.length){const current=queue.shift();for(const edge of data.relationships||[]){if(edge.type!==type||edge.source!==current.id&&edge.target!==current.id)continue;const directed=isDirectedRelation(type);if(directed&&edge.source!==current.id)continue;const other=map.get(edge.source===current.id?edge.target:edge.source);if(!other)continue;const edgeKey=edge.id||[edge.type,edge.source,edge.target].join('|');if(!actualSeen.has(edgeKey)){actualSeen.add(edgeKey);actual.push({id:edgeKey,from:map.get(edge.source)||current,to:map.get(edge.target)||other,type,directed})}if(!walked.has(other.id)){walked.add(other.id);queue.push(other)}}}const members=[...walked].map(id=>map.get(id)).filter(item=>item&&(item.type==='account'||item.type==='ib_user')),ids=new Set(members.map(item=>item.id));if(isDirectedRelation(type))return{ids,edges:actual.filter(edge=>ids.has(edge.from.id)&&ids.has(edge.to.id)),type};return{ids,edges:members.filter(item=>item.id!==seed.id).map(item=>({id:'cluster|'+type+'|'+seed.id+'|'+item.id,from:seed,to:item,type,directed:false})),type}}function selectedBranch(node){const ancestry=route(node),cluster=relationshipCluster(node),ids=new Set([...ancestry.map(item=>item.id),...cluster.ids]),edges=[],seen=new Set(),children=new Map();for(const item of graphNodes()){const owner=parent(item);if(owner&&owner.id!==item.id){if(!children.has(owner.id))children.set(owner.id,[]);children.get(owner.id).push(item)}}function append(edge){const key=edge.id||[edge.type,edge.from.id,edge.to.id].join('|');if(!seen.has(key)){seen.add(key);edges.push(edge)}}for(let index=1;index<ancestry.length;index++){const to=ancestry[index],from=ancestry[index-1],type=branchType(to);append({id:'path|'+from.id+'|'+to.id+'|'+type,from,to,type,directed:isDirectedRelation(type)})}for(const edge of cluster.edges)append(edge);const queue=[...cluster.ids].map(id=>byId().get(id)).filter(Boolean),visited=new Set(cluster.ids);while(queue.length){const owner=queue.shift();for(const child of children.get(owner.id)||[]){ids.add(child.id);append({id:'tree|'+owner.id+'|'+child.id+'|'+branchType(child),from:owner,to:child,type:branchType(child),directed:isDirectedRelation(branchType(child))});if(!visited.has(child.id)){visited.add(child.id);queue.push(child)}}}return{ids,edges,type:cluster.type}}function rootChainEdges(nodes){const subject=accounts().find(item=>item.isSubject),edges=[],seen=new Set();if(!subject)return edges;for(const node of nodes){if(node.id===subject.id)continue;const chain=route(node);if(chain[0]?.id!==subject.id)continue;for(let index=1;index<chain.length;index++){const from=chain[index-1],to=chain[index],type=branchType(to),key=[from.id,to.id,type].join('|');if(seen.has(key))continue;seen.add(key);edges.push({id:'root|'+key,from,to,type,directed:isDirectedRelation(type)})}}return edges}
function color(node){if(node.isSubject)return'#ff2638';if(!node.expandable)return'#35c285';const score=Math.max(0,Math.min(100,Number(node.score||0))),hue=2+Math.pow((100-score)/100,.72)*72,light=48+Math.min(12,(100-score)*.12);return `hsl(${hue} 96% ${light}%)`}function relationKey(type){const aliases={same_crm:'same_crm_user',sameCrm:'same_crm_user',shared_last_ip:'login_ip',sharedLastIp:'login_ip',same_ip:'login_ip',sameIp:'login_ip',shared_cid:'client_id',sharedCid:'client_id',same_cid:'client_id',sameCid:'client_id',ea:'ea_feature',copy:'copy_order',copy_orders:'copy_order',copy_groups:'copy_group',sameName:'same_name',top_ib_group:'ib_group',direct_ib:'ib_group',crm_owner:'ib_group'};return aliases[type]||type||'unknown'}function relationTheme(type){const palette={same_crm_user:['#60a5fa','rgba(59,130,246,.18)'],login_ip:['#a78bfa','rgba(139,92,246,.18)'],client_id:['#c084fc','rgba(168,85,247,.18)'],ea_feature:['#22d3ee','rgba(6,182,212,.18)'],copy_order:['#f472b6','rgba(236,72,153,.18)'],copy_group:['#e879f9','rgba(217,70,239,.18)'],rebate:['#fbbf24','rgba(245,158,11,.18)'],ib_direct_rebate:['#818cf8','rgba(99,102,241,.18)'],ib_direct_account:['#818cf8','rgba(99,102,241,.18)'],ib_owned_account:['#818cf8','rgba(99,102,241,.18)'],ib_identity:['#818cf8','rgba(99,102,241,.18)'],ib_group:['#818cf8','rgba(99,102,241,.18)'],same_name:['#2dd4bf','rgba(20,184,166,.18)'],toxic_sync_same:['#fb7185','rgba(244,63,94,.18)'],toxic_sync_opposite:['#f43f5e','rgba(244,63,94,.22)']},item=palette[relationKey(type)]||['#94a3b8','rgba(100,116,139,.16)'];return{stroke:item[0],fill:item[1]}}function relationShortLabel(type){const labels={same_crm_user:'同名账户',login_ip:'同 LastIP',client_id:'同 CID',ea_feature:'同 EA',copy_order:'跟单订单匹配（开仓/平仓）',copy_group:'跟单来源组匹配',rebate:'返佣',ib_direct_rebate:'IB 直接返佣',ib_direct_account:'直属 IB 本人账户',ib_owned_account:'IB 本人账户',ib_identity:'IB 身份',ib_group:'IB 群组',same_name:'同名',toxic_sync_same:'Toxic 同向',toxic_sync_opposite:'Toxic 反向',unknown:'其他关系'};return labels[relationKey(type)]||String(type)}function localAction(node){const raw=String(node&&node.localAction||'').trim().toUpperCase();if(!raw||raw==='待定')return'B';return raw==='A/TA'?'TA':raw}function actionTheme(action){const colors={B:'#94a3b8',P:'#38bdf8',T:'#f59e0b',TA:'#fb7185',A:'#ef4444'};return colors[action]||'#a78bfa'}function isAttentionAction(action){return action==='T'||action==='TA'||action==='A'||action==='A/T'}function nodeShape(node){if(node.isSubject)return'problem';if(!node.expandable)return'diamond';return node.type==='ib_user'?'hexagon':'circle'}
function drawNode(node,x,y,size){ctx.beginPath();const shape=nodeShape(node);if(shape==='hexagon'){for(let point=0;point<6;point++){const angle=-Math.PI/2+point*Math.PI/3,px=x+Math.cos(angle)*size,py=y+Math.sin(angle)*size;point?ctx.lineTo(px,py):ctx.moveTo(px,py)}ctx.closePath()}else if(shape==='diamond'){ctx.moveTo(x,y-size);ctx.lineTo(x+size,y);ctx.lineTo(x,y+size);ctx.lineTo(x-size,y);ctx.closePath()}else ctx.arc(x,y,size,0,Math.PI*2)}function drawTerminalBadge(state,x,y,size){if(!state)return;const radius=Math.max(6,size+2),label=state==='leaf'?'叶':'止';ctx.beginPath();ctx.arc(x+size+4,y-size-4,radius,0,Math.PI*2);ctx.fillStyle=state==='leaf'?'#0f766e':'#166534';ctx.fill();ctx.strokeStyle='#a7f3d0';ctx.lineWidth=1.8;ctx.stroke();ctx.fillStyle='#f0fdf4';ctx.font='bold 8px Microsoft YaHei';ctx.textAlign='center';ctx.fillText(label,x+size+4,y-size+2);ctx.textAlign='start'}function drawActionBadge(node,x,y,size){if(node.type!=='account')return;const action=localAction(node),attention=isAttentionAction(action),radius=Math.max(5,Math.min(10,size*.52));if(attention){ctx.beginPath();ctx.arc(x,y,size+3,0,Math.PI*2);ctx.strokeStyle=actionTheme(action);ctx.globalAlpha=.9;ctx.lineWidth=2.3;ctx.stroke();ctx.globalAlpha=1}ctx.beginPath();ctx.arc(x,y,radius,0,Math.PI*2);ctx.fillStyle='rgba(11,18,32,.92)';ctx.fill();ctx.strokeStyle=actionTheme(action);ctx.lineWidth=attention?1.7:1.1;ctx.stroke();ctx.fillStyle=actionTheme(action);ctx.font=(attention?'bold ':'600 ')+Math.max(6,Math.min(10,size*.62))+'px Microsoft YaHei';ctx.textAlign='center';ctx.fillText(action,x,y+Math.max(2,Math.min(3,size*.18)));ctx.textAlign='start'}function drawGroupLabel(group,center){if(group.nodes.length<2||group.end-group.start<.24)return;const angle=(group.start+group.end)/2,radius=group.radius+30,theme=relationTheme(group.type),text=relationShortLabel(group.type);ctx.save();ctx.fillStyle=theme.stroke;ctx.font='bold 10px Microsoft YaHei';ctx.textAlign='center';ctx.fillText(text,center.x+Math.cos(angle)*radius,center.y+Math.sin(angle)*radius+3);ctx.restore()}function drawGroupBand(group,center){if(group.nodes.length<2)return;const active=group.key===activeGroupKey,theme=relationTheme(group.type),inner=Math.max(0,group.radius-17),outer=group.radius+17;ctx.beginPath();ctx.arc(center.x,center.y,outer,group.start,group.end);ctx.arc(center.x,center.y,inner,group.end,group.start,true);ctx.closePath();ctx.fillStyle=theme.fill;ctx.fill();ctx.strokeStyle=theme.stroke;ctx.lineWidth=1.35;ctx.stroke();if(active){ctx.beginPath();ctx.arc(center.x,center.y,outer+3,group.start,group.end);ctx.arc(center.x,center.y,Math.max(0,inner-3),group.end,group.start,true);ctx.closePath();ctx.strokeStyle='#f8fafc';ctx.lineWidth=1.8;ctx.setLineDash([5,4]);ctx.stroke();ctx.setLineDash([])}drawGroupLabel(group,center)}
function relationEdgeLabel(edge){if(!edge.directed)return relationShortLabel(edge.type);const names={direct_ib:'直属上级 IB',ib_direct_account:'直属上级 IB 本人账户',ib_owned_account:'IB 本人名下账户',ib_identity:'IB 身份归属',ib_direct_rebate:'IB 直接返佣人员',crm_owner:'CRM 账户归属'};return String(edge.from.label)+' → '+String(edge.to.label)+'（'+(names[edge.type]||relationShortLabel(edge.type))+'）'}function drawRelationEdge(edge){const from=layout.get(edge.from.id),to=layout.get(edge.to.id);if(!from||!to)return;const theme=relationTheme(edge.type),dx=to.x-from.x,dy=to.y-from.y,length=Math.hypot(dx,dy);if(length<18)return;ctx.save();ctx.strokeStyle=theme.stroke;ctx.globalAlpha=.92;ctx.lineWidth=2.4;ctx.beginPath();ctx.moveTo(from.x,from.y);ctx.lineTo(to.x,to.y);ctx.stroke();const angle=Math.atan2(dy,dx);if(edge.directed){const tip=Math.max(7,Math.min(14,length*.14)),backX=to.x-Math.cos(angle)*tip,backY=to.y-Math.sin(angle)*tip;ctx.globalAlpha=1;ctx.fillStyle=theme.stroke;ctx.beginPath();ctx.moveTo(to.x,to.y);ctx.lineTo(backX+Math.cos(angle+2.55)*6,backY+Math.sin(angle+2.55)*6);ctx.lineTo(backX+Math.cos(angle-2.55)*6,backY+Math.sin(angle-2.55)*6);ctx.closePath();ctx.fill()}const label=relationEdgeLabel(edge),mx=(from.x+to.x)/2,my=(from.y+to.y)/2,offset=Math.min(18,Math.max(9,length*.12)),lx=mx-Math.sin(angle)*offset,ly=my+Math.cos(angle)*offset;ctx.globalAlpha=1;ctx.font='bold 10px Microsoft YaHei';const width=ctx.measureText(label).width+10;ctx.fillStyle='rgba(11,18,32,.9)';ctx.fillRect(lx-width/2,ly-8,width,16);ctx.fillStyle=theme.stroke;ctx.textAlign='center';ctx.fillText(label,lx,ly+3);ctx.restore();ctx.textAlign='start'}function relationshipGroup(node){if(node.isSubject)return'subject';const owner=parent(node);return (owner?owner.id:'root')+'|'+branchType(node)}function ringLayout(all,w,h){const subject=all.find(node=>node.isSubject),center={x:w*.5,y:h*.5},depths=all.filter(node=>!node.isSubject).map(accountDepth),maxDepth=depths.length?Math.max(1,Math.min(4,...depths)):1,availableRadius=Math.max(72,Math.min(w*.46,h*.45)),baseRadius=availableRadius/maxDepth,rings=new Map(),groupsByDepth=new Map();for(let depth=1;depth<=4;depth++){const groups=new Map();for(const node of all.filter(item=>!item.isSubject&&Math.min(4,accountDepth(item))===depth)){const key=relationshipGroup(node);if(!groups.has(key))groups.set(key,{key,nodes:[],type:branchType(node),owner:parent(node)});groups.get(key).nodes.push(node)}const ordered=[...groups.values()].sort((a,b)=>String(a.owner?.label||'').localeCompare(String(b.owner?.label||''),undefined,{numeric:true})||String(a.type).localeCompare(String(b.type)));for(const group of ordered)group.nodes.sort((a,b)=>String(a.label).localeCompare(String(b.label),undefined,{numeric:true}));rings.set(depth,ordered.flatMap(group=>group.nodes));groupsByDepth.set(depth,ordered)}const radii=new Map();let previous=0;for(let depth=1;depth<=4;depth++){const count=(rings.get(depth)||[]).length,minRadius=count?count*14/(Math.PI*2):0,radius=Math.max(baseRadius*depth,minRadius,previous+58);radii.set(depth,radius);previous=radius}layout=new Map();if(subject)layout.set(subject.id,{...center,depth:0,type:'subject',groupKey:'subject'});for(let depth=1;depth<=4;depth++){const groups=groupsByDepth.get(depth)||[],radius=radii.get(depth),slots=groups.reduce((sum,group)=>sum+group.nodes.length+(group.nodes.length>1?1.1:0),0)||1,step=Math.PI*2/slots;let cursor=0;for(const group of groups){const padding=group.nodes.length>1?.55:0,start=-Math.PI/2+(cursor-padding)*step,end=-Math.PI/2+(cursor+group.nodes.length-1+padding)*step;group.start=start;group.end=end;group.radius=radius;for(const [index,node] of group.nodes.entries()){const angle=-Math.PI/2+(cursor+index)*step;layout.set(node.id,{x:center.x+Math.cos(angle)*radius,y:center.y+Math.sin(angle)*radius,depth,type:group.type,groupKey:group.key})}cursor+=group.nodes.length+(group.nodes.length>1?1.1:.55)}}return{subject,center,rings,radii,groupsByDepth}}
function renderOverview(){if(!data)return;const r=canvas.getBoundingClientRect(),w=r.width,h=r.height,all=graphNodes(),placement=ringLayout(all,w,h),subject=placement.subject;if(!subject)return;const selected=byId().get(selectedId)||subject,branch=selectedId&&selected.id!==subject.id?selectedBranch(selected):{ids:new Set(route(selected).map(item=>item.id)),edges:[]},rootEdges=rootChainEdges(all),rootKeys=new Set(rootEdges.map(edge=>[edge.from.id,edge.to.id,edge.type].join('|'))),extraBranchEdges=branch.edges.filter(edge=>!rootKeys.has([edge.from.id,edge.to.id,edge.type].join('|'))),activeNodes=all.filter(node=>layout.get(node.id)?.groupKey===activeGroupKey),activeText=activeNodes.length?'；当前高亮 '+activeNodes.length+' 个同组节点':'',branchText=branch.edges.length?'；已显示「'+selected.label+'」后续扩散分支 '+branch.edges.length+' 段关系线（线色和文字均表示关系）':'';document.getElementById('overviewNote').textContent='概览已单独排布 '+all.length+' 个发现节点（账户 '+accounts().length+' 个）；每个节点均显示至主账户的关联链路；同一圈内按关系来源成簇、簇间留空'+activeText+branchText+'。绿色「叶」表示已核查叶节点；节点内为本地标记，T／TA／A 有高亮环；滚轮缩放或点击节点可查看路径。';ctx.fillStyle='#101826';ctx.fillRect(0,0,w,h);ctx.save();ctx.translate(view.x,view.y);ctx.scale(view.scale,view.scale);for(let ring=1;ring<=4;ring++){const radius=placement.radii.get(ring),items=placement.rings.get(ring)||[];ctx.strokeStyle='rgba(112,144,178,.45)';ctx.lineWidth=1;ctx.setLineDash([4,6]);ctx.beginPath();ctx.arc(placement.center.x,placement.center.y,radius,0,Math.PI*2);ctx.stroke();ctx.setLineDash([]);for(const group of placement.groupsByDepth.get(ring)||[])drawGroupBand(group,placement.center);ctx.fillStyle='#93acc6';ctx.font='11px Microsoft YaHei';ctx.fillText('第 '+ring+' 圈 · '+items.length+' 个节点',placement.center.x-radius-58,placement.center.y-radius+7)}for(const edge of rootEdges)drawRelationEdge(edge);for(const edge of extraBranchEdges)drawRelationEdge(edge);for(const node of all){const p=layout.get(node.id),depth=p.depth,current=node.id===selectedId,onBranch=branch.ids.has(node.id),inActiveGroup=p.groupKey===activeGroupKey,terminal=terminalState(node),size=node.isSubject?19:node.type==='ib_user'?11:depth===1?10.5:depth===2?8.5:7;if(current||inActiveGroup&&!node.isSubject){ctx.strokeStyle=current?'rgba(88,196,255,.52)':'rgba(125,211,252,.82)';ctx.lineWidth=current?9:3.4;ctx.beginPath();ctx.arc(p.x,p.y,current?21:size+5,0,Math.PI*2);ctx.stroke()}else if(onBranch){ctx.strokeStyle='rgba(88,196,255,.28)';ctx.lineWidth=5.5;ctx.beginPath();ctx.arc(p.x,p.y,size+5,0,Math.PI*2);ctx.stroke()}ctx.fillStyle=color(node);ctx.globalAlpha=depth>=3?.9:1;drawNode(node,p.x,p.y,current?11:size);ctx.fill();ctx.globalAlpha=1;if(node.type==='ib_user'){ctx.strokeStyle='#7dd3fc';ctx.lineWidth=1.6;drawNode(node,p.x,p.y,current?11:size);ctx.stroke()}drawActionBadge(node,p.x,p.y,current?11:size);drawTerminalBadge(terminal,p.x,p.y,current?11:size);if(current||node.isSubject){ctx.strokeStyle='#fff';ctx.lineWidth=2.2;drawNode(node,p.x,p.y,current?11:size);ctx.stroke();ctx.fillStyle='#fff';ctx.font='500 12px Microsoft YaHei';ctx.textAlign='center';ctx.fillText(node.label,p.x,p.y-(node.isSubject?27:21));ctx.textAlign='start'}}ctx.restore();positionRadar()}function typeGroups(node){const result=[],labels=relationMap();for(const edge of data.relationships){if(edge.source===node.id||edge.target===node.id){if(!result.includes(edge.type))result.push(edge.type)}}for(const ledger of node.scoreLedger||[]){if(!result.includes(ledger.type))result.push(ledger.type)}return result.sort((a,b)=>(labels.get(a)||a).localeCompare(labels.get(b)||b))}function peers(node,type){const map=byId(),ids=new Set(),connectors=new Set([node.id]),directTypes=new Set(['ib_direct_account','ib_direct_rebate']);if(directTypes.has(type)){for(const edge of data.relationships)if(edge.type===type&&(edge.source===node.id||edge.target===node.id)){const other=edge.source===node.id?edge.target:edge.source;const item=map.get(other);if(item&&item.type==='account'&&item.id!==node.id)ids.add(item.id)}}else{for(const edge of data.relationships)if(edge.type===type&&(edge.source===node.id||edge.target===node.id))connectors.add(edge.source===node.id?edge.target:edge.source);for(const connector of connectors)for(const edge of data.relationships)if(edge.type===type&&(edge.source===connector||edge.target===connector)){const other=edge.source===connector?edge.target:edge.source;const item=map.get(other);if(item&&item.type==='account'&&item.id!==node.id)ids.add(item.id)}}return [...ids].map(id=>map.get(id)).sort((a,b)=>Number(b.score)-Number(a.score)).slice(0,8)}
function relationshipTitle(type,labels){const titles={login_ip:'当前登录 IP 相同',client_id:'当前 CID 相同',top_ib_group:'同属顶级 IB 群组（仅聚合）',rebate:'存在返佣链路记录',ib_direct_account:'直属上级 IB 本人交易账户',ib_owned_account:'IB 本人名下交易账户',ib_identity:'IB 身份确认',ib_direct_rebate:'IB 直接返佣人员',direct_ib:'CRM 直属上级 IB',crm_owner:'CRM 账户归属',same_crm_user:'同名账户',ea_feature:'EA / 路由特征相同',copy_order:'跟单订单匹配（开仓/平仓）',copy_group:'跟单来源组匹配',toxic_sync_same:'Toxic 同向开平仓时间匹配',toxic_sync_opposite:'Toxic 反向开平仓时间匹配'};return titles[type]||labels.get(type)||type}
function relationshipExplanation(type){const explanations={login_ip:'该账户与问题账户当前 LastIP 相同；仅代表当前登录 IP 重合，不等同于同一设备。',client_id:'该账户与问题账户在同一 MT5 服务器上的当前 CID 相同；CID 为 0 或空值时不建立关系。',top_ib_group:'与问题账户同属同一顶级 IB 群组；只显示群组统计，不会自动把全部下属账户扩散出来。',rebate:'问题账户与这些账户之间存在返佣收款或返佣链路记录。',ib_direct_account:'问题账户的直属上级 IB 本人名下交易账户，不是该 IB 的下属客户账户。',ib_owned_account:'该交易账户归属该 IB 本人。',ib_identity:'当前交易账户对应的客户身份就是该 IB；这条身份桥不衰减分数。',ib_direct_rebate:'该账户是该 IB 的直接返佣人员账户；它达到阈值后会继续按 IP、CID、EA、跟单、同名账户和返佣规则扩散。',direct_ib:'客户层级显示该 IB 是问题账户的直属上级。',crm_owner:'该交易账户对应问题账户所属客户，用于解释账户归属。',same_crm_user:'这些是登记在同一客户名下的同名账户。',ea_feature:'订单 Comment / ExpertID / 路由特征存在匹配。',copy_order:'该关系来自订单跟单匹配：系统识别两个账户存在符合规则的对应开仓和/或平仓订单，不等同于只因时间接近。请核对匹配订单数量、品种、方向及时间。',copy_group:'该关系来自同一已识别跟单来源组；它表示来源归组一致，不单独证明每笔订单都逐笔同步。',toxic_sync_same:'该关系来自 Toxic 同向开平仓时间匹配：两个账户在相同方向的订单开仓、平仓时间满足同步规则。',toxic_sync_opposite:'该关系来自 Toxic 反向开平仓时间匹配：两个账户订单的开仓、平仓时间满足同步规则，但方向相反。'};return explanations[type]||'这是已读取到的关系证据；请结合右侧信息核验。'}
function relationPathText(node){if(node.isSubject)return'这是问题账户，调查从该账户开始。';const nodes=route(node),labels=relationMap(),steps=[];for(let index=1;index<nodes.length;index++){const item=nodes[index],kind=item.type==='ib_user'?'IB 节点':'账户';steps.push('通过「'+relationshipTitle(branchType(item),labels)+'」识别到'+kind+' '+item.label)}return '从问题账户 '+(nodes[0]?nodes[0].label:node.label)+' 出发，'+(steps.length?steps.join('；'):'识别到该关联节点')+'。'}
function renderDetail(){const node=byId().get(selectedId);if(!node)return;const depth=accountDepth(node),labels=relationMap(),types=typeGroups(node),kind=node.type==='ib_user'?'IB 身份节点':'账户',action=node.type==='account'?localAction(node):'';if(!types.length)types.push(branchType(node));if(!types.includes(activeType))activeType=types[0];document.getElementById('location').textContent='当前查看：'+node.label+(node.isSubject?'（问题账户 / 调查起点）':'（'+kind+' / 与问题账户相隔第 '+depth+' 层）');document.getElementById('path').textContent='关系路径说明：'+relationPathText(node);document.getElementById('selected').textContent=node.label;document.getElementById('selectedNote').textContent=(node.isSubject?'问题账户 / 第 0 层 / 传播分 100.0':kind+' / 第 '+depth+' 层 / 传播分 '+Number(node.score||0).toFixed(1))+(action?' / 本地标记 '+action:'')+(checkedLeaf(node)?' / 已核查，无新增账户':'');groups.replaceChildren();for(const type of types){const button=document.createElement('button');button.type='button';button.className='group'+(type===activeType?' active':'');const title=document.createElement('strong');title.textContent=relationshipTitle(type,labels);const note=document.createElement('span');note.textContent=relationshipExplanation(type);button.append(title,note);button.addEventListener('click',()=>{activeType=type;renderDetail()});groups.append(button)}const related=peers(node,activeType),edge=data.relationships.find(x=>x.type===activeType&&(x.source===node.id||x.target===node.id)),explanation=relationshipExplanation(activeType),detail=activeType==='same_crm_user'?'':((edge&&edge.evidence&&edge.evidence[0])?(' 线索：'+edge.evidence[0]):(' 当前显示 '+related.length+' 个关联账户'));document.getElementById('evidence').textContent='关联逻辑：'+relationshipTitle(activeType,labels);document.getElementById('evidenceNote').textContent=explanation+detail;people.replaceChildren();if(!related.length){const empty=document.createElement('div');empty.className='empty';empty.textContent='该线索未发现可显示的其他账户。';people.append(empty)}for(const item of related){const card=document.createElement('div');card.className='person'+(!item.expandable?' stop':accountDepth(item)>1?' mid':'');const name=document.createElement('strong');name.textContent=item.label+' · '+localAction(item);const note=document.createElement('small');note.textContent='与问题账户相隔第 '+accountDepth(item)+' 层 · 传播分 '+Number(item.score||0).toFixed(1)+(checkedLeaf(item)?' / 已核查，无新增账户':'');card.append(name,note);card.addEventListener('click',()=>{selectedId=item.id;activeType='';renderOverview();renderDetail()});people.append(card)}}
function overviewHeight(){const maxDepth=data?Math.max(1,...graphNodes().map(node=>Math.min(4,accountDepth(node)))):1;return Math.min(820,Math.max(470,220+maxDepth*145))}function fit(){canvas.parentElement.style.height=overviewHeight()+'px';const r=canvas.getBoundingClientRect(),d=devicePixelRatio||1;canvas.width=Math.round(r.width*d);canvas.height=Math.round(r.height*d);ctx.setTransform(d,0,0,d,0,0);renderOverview();fitMapToBoard();renderOverview()}function world(ev){const r=canvas.getBoundingClientRect();return{x:(ev.clientX-r.left-view.x)/view.scale,y:(ev.clientY-r.top-view.y)/view.scale}}function fitMapToBoard(){const subject=accounts().find(node=>node.isSubject),point=subject&&layout.get(subject.id),bounds=canvas.getBoundingClientRect();if(!point||!bounds.width||!bounds.height)return;const outer=Math.max(...[...layout.values()].map(item=>Math.hypot(item.x-point.x,item.y-point.y)),1);const scale=Math.max(.10,Math.min(2.5,Math.min(bounds.width,bounds.height)*.43/(outer+20)));view.scale=scale;view.x=bounds.width*.5-point.x*scale;view.y=bounds.height*.5-point.y*scale}window.addEventListener('resize',fit);canvas.addEventListener('wheel',ev=>{ev.preventDefault();const r=canvas.getBoundingClientRect(),before=world(ev),factor=ev.deltaY<0?1.12:1/1.12,next=Math.max(.10,Math.min(2.5,view.scale*factor));view.scale=next;view.x=ev.clientX-r.left-before.x*next;view.y=ev.clientY-r.top-before.y*next;renderOverview()},{passive:false});canvas.addEventListener('pointerdown',ev=>{drag={x:ev.clientX,y:ev.clientY,viewX:view.x,viewY:view.y};canvas.setPointerCapture(ev.pointerId)});canvas.addEventListener('pointermove',ev=>{if(!drag)return;view.x=drag.viewX+ev.clientX-drag.x;view.y=drag.viewY+ev.clientY-drag.y;renderOverview()});canvas.addEventListener('pointerup',()=>{drag=null});canvas.addEventListener('dblclick',()=>{fitMapToBoard();renderOverview()});canvas.addEventListener('click',ev=>{if(!data)return;const point=world(ev);let closest=null,distance=15/Math.max(view.scale,.1);for(const node of graphNodes()){const p=layout.get(node.id),d=p?Math.hypot(point.x-p.x,point.y-p.y):Infinity;if(d<distance){closest=node;distance=d}}if(closest){selectedId=closest.id;activeGroupKey=layout.get(closest.id)?.groupKey||'';activeType='';renderOverview();renderDetail()}});
const params=new URLSearchParams(location.search),target=params.get('account')||'',includeToxic=document.getElementById('includeToxic');let controller=null,pollTimer=null;if(target)document.getElementById('back').href='/account/'+encodeURIComponent(target)+'?'+params.toString();function apiUrl(value){if(!target)return '/api/kuzu-risk/graph?threshold='+value;const query=new URLSearchParams();for(const key of ['platform','server','symbol'])if(params.get(key))query.set(key,params.get(key));query.set('threshold',String(value));if(includeToxic.checked)query.set('include_toxic','true');return '/api/accounts/by-login/'+encodeURIComponent(target)+'/relationship-network?'+query.toString()}function queuePoll(){clearTimeout(pollTimer);pollTimer=setTimeout(()=>load(true),1200)}async function load(isPoll=false){if(controller)return;controller=new AbortController();const active=controller,value=Math.max(1,Math.min(100,Number(threshold.value)||20));threshold.value=value;if(!isPoll)status.textContent=includeToxic.checked?'后台扩散与 Toxic 同向/反向开平仓时间匹配准备中…':'关系后台扩散准备中…';const timeout=setTimeout(()=>active.abort(),8000);try{const response=await fetch(apiUrl(value),{cache:'no-store',signal:active.signal});if(!response.ok)throw new Error();const next=await response.json();const nextNodes=Array.isArray(next.entities)?next.entities.filter(x=>x.type==='account'||x.type==='ib_user'):[];if(!next.ok){status.textContent=(next.limitations||[])[0]||'图谱读取失败';return}if(!nextNodes.length){status.textContent='关系后台扩散中：等待首批证据…';queuePoll();return}const hasSelected=nextNodes.some(x=>x.id===selectedId);data=next;if(!hasSelected){selectedId=data.subjectId||nextNodes.find(x=>x.isSubject)?.id||'';activeType='';view={scale:1,x:0,y:0}}fit();renderDetail();const progress=data.progress||{},expanded=Number(progress.expandedAccounts||data.summary?.discoveryAccountCount||0),pending=Number(progress.pendingAccounts||data.summary?.pendingAccountCount||0);if(data.inProgress){status.textContent='后台扩散中：已处理 '+expanded+' 个账户，待处理 '+pending+' 个；图谱会自动更新。';queuePoll()}else{status.textContent='规则 '+data.summary.ruleVersion+(data.truncated?' · 已触发安全节点/边上限':' · 已按阈值完成扩散')}}catch(error){status.textContent=error.name==='AbortError'?'图谱请求超时，后台任务仍会继续；正在重试。':'图谱读取失败';queuePoll()}finally{clearTimeout(timeout);if(controller===active)controller=null}}document.getElementById('reload').addEventListener('click',()=>{clearTimeout(pollTimer);selectedId='';activeType='';load()});load();
function rootChainEdges(){const selected=byId().get(selectedId);return selected&&!selected.isSubject?selectedBranch(selected).edges:[]}
function positionRadar(){const subject=accounts().find(node=>node.isSubject),point=subject&&layout.get(subject.id),bounds=canvas.getBoundingClientRect();if(!point||!bounds.width||!bounds.height)return;const selected=byId().get(selectedId)||subject,branch=selected&&!selected.isSubject?selectedBranch(selected):{edges:[]},activeNodes=graphNodes().filter(node=>layout.get(node.id)?.groupKey===activeGroupKey),activeText=activeNodes.length?'；当前高亮 '+activeNodes.length+' 个同组节点':'',branchText=branch.edges.length?'；当前账户直接关系 '+branch.edges.length+' 段，已附主账户回溯路径':'';document.getElementById('overviewNote').textContent='概览仅显示当前账户与主账户的路径和直接关系，不把仅通过主账户相连的节点互相高亮'+activeText+branchText+'。绿色「叶」表示已核查叶节点；节点中央为风控数据库状态（无状态为 B），T／TA／A 有高亮环。';const radius=Math.hypot(bounds.width,bounds.height),angle=Math.PI/10,endX=Math.sin(angle)*radius,endY=-Math.cos(angle)*radius,x=point.x*view.scale+view.x,y=point.y*view.scale+view.y;radarSvg.setAttribute('viewBox','0 0 '+bounds.width+' '+bounds.height);radarFan.setAttribute('d','M0 0 L0 -'+radius+' A '+radius+' '+radius+' 0 0 1 '+endX+' '+endY+' Z');radarBeam.setAttribute('y2',String(-radius));radarSweep.setAttribute('transform','translate('+x+' '+y+')')}
function databaseStatus(node){const raw=String(node&&node.databaseStatus||'').trim().toUpperCase();if(!raw||raw==='-'||raw==='待定')return'B';return raw==='A/TA'?'TA':raw}
function localAction(node){return databaseStatus(node)}
const renderDetailWithDatabaseStatus=renderDetail;renderDetail=function(){renderDetailWithDatabaseStatus();const note=document.getElementById('selectedNote');if(note)note.textContent=note.textContent.replace('本地标记','数据库状态')}
const NODE_SCALE=2,drawScaledNode=drawNode,drawScaledStatusBadge=drawActionBadge,drawScaledTerminalBadge=drawTerminalBadge;drawNode=function(node,x,y,size){drawScaledNode(node,x,y,size*NODE_SCALE)};drawActionBadge=function(node,x,y,size){drawScaledStatusBadge(node,x,y,size*NODE_SCALE)};drawTerminalBadge=function(state,x,y,size){drawScaledTerminalBadge(state,x,y,size*NODE_SCALE)}
canvas.addEventListener('click',ev=>{if(!data)return;const point=world(ev),hitRadius=26/Math.max(view.scale,.1);let closest=null,distance=hitRadius;for(const node of graphNodes()){const p=layout.get(node.id),candidate=p?Math.hypot(point.x-p.x,point.y-p.y):Infinity;if(candidate<distance){closest=node;distance=candidate}}if(closest){selectedId=closest.id;activeGroupKey=layout.get(closest.id)?.groupKey||'';activeType='';renderOverview();renderDetail()}});
const copyInspector=document.getElementById('copyInspector'),copyInspectorTitle=document.getElementById('copyInspectorTitle'),copyInspectorNote=document.getElementById('copyInspectorNote'),copyInspectorContent=document.getElementById('copyInspectorContent'),copyEdgeTab=document.getElementById('copyEdgeTab'),copyMasterTab=document.getElementById('copyMasterTab');let copyInspection=null,copyInspectionTab='edge',copyHitEdges=[];function copyValue(value){return value===undefined||value===null||value===''?'-':String(value)}function copyTable(headers,rows){const table=document.createElement('table');table.className='copy-inspector-table';const head=document.createElement('thead'),headRow=document.createElement('tr');for(const label of headers){const cell=document.createElement('th');cell.textContent=label;headRow.append(cell)}head.append(headRow);const body=document.createElement('tbody');for(const row of rows){const item=document.createElement('tr');for(const value of row){const cell=document.createElement('td');cell.textContent=copyValue(value);item.append(cell)}body.append(item)}table.append(head,body);return table}function renderCopyInspection(){if(!copyInspection)return;const{master,follower,origin,payload}=copyInspection,followers=Array.isArray(origin?.followers)?origin.followers:[],followerOrders=Array.isArray(origin?.followerOrders)?origin.followerOrders:[],sourceOrders=Array.isArray(origin?.sourceOrders)?origin.sourceOrders:[],sourceById=new Map(sourceOrders.map(item=>[String(item.orderId||item.ticket||''),item]));copyInspectorContent.replaceChildren();copyEdgeTab.classList.toggle('active',copyInspectionTab==='edge');copyMasterTab.classList.toggle('active',copyInspectionTab==='master');if(copyInspectionTab==='edge'){const orders=followerOrders.filter(item=>String(item.account||'')===String(follower.label));const summary=document.createElement('div');summary.className='copy-inspector-summary';for(const text of ['跟随账户 '+follower.label,'单主 '+master.label,'匹配订单 '+orders.length+' 笔',origin?.platform||master.platform||'-',''+(origin?.server||master.server||'-')]){const chip=document.createElement('span');chip.textContent=text;summary.append(chip)}copyInspectorContent.append(summary);if(!orders.length){const empty=document.createElement('div');empty.className='copy-inspector-muted';empty.textContent='该关系已识别到跟单订单，但当前筛选范围内没有可展示的逐笔匹配记录。';copyInspectorContent.append(empty);return}copyInspectorContent.append(copyTable(['单主订单','跟随账户订单','品种','开仓时间','平仓时间','手数','净盈亏'],orders.map(item=>{const ids=item.matchedSourceOrderIds||item.matchedOrderIds||[],source=sourceById.get(String(ids[0]||''))||{};return[(source.ticket||source.orderId||ids.join(', ')),item.ticket||'-',item.symbol||source.symbol||'-',item.openTime||item.time||source.time||'-',item.closeTime||'-',item.volume||'-',item.netProfit??item.grossProfit??'-']})));return}const summary=document.createElement('div');summary.className='copy-inspector-summary';for(const text of ['单主 '+master.label,'跟随账户 '+followers.length+' 个','跟单订单 '+(origin?.followerSummary?.orders??followerOrders.length)+' 笔','净盈亏 '+(origin?.followerSummary?.netProfit??'-')+' '+(origin?.followerSummary?.currency||'')]){const chip=document.createElement('span');chip.textContent=text;summary.append(chip)}copyInspectorContent.append(summary);if(!followers.length){const empty=document.createElement('div');empty.className='copy-inspector-muted';empty.textContent='当前筛选范围内未发现该单主的其他跟随账户。';copyInspectorContent.append(empty);return}copyInspectorContent.append(copyTable(['跟随账户','平台 / 服务器','匹配单主订单','跟单订单','品种','首次 / 最后记录','净盈亏'],followers.map(item=>[item.account,(item.platform||'-')+' / '+(item.server||'-'),item.matchedSourceOrders??'-',item.orders??'-',(item.symbols||[]).join(', ')||'-',(item.firstTime||'-')+' / '+(item.lastTime||'-'),(item.netProfit??'-')+' '+(item.displayCurrency||item.currency||'')])))}async function fetchCopyInspection(edge){const source=byId().get(edge.source),targetNode=byId().get(edge.target),sourceIsMaster=source?.detail==='跟单单主',master=sourceIsMaster?source:targetNode,follower=sourceIsMaster?targetNode:source;if(!master||!follower)return;copyInspector.classList.add('open');copyInspectorTitle.textContent='跟单订单明细';copyInspectorNote.textContent='正在读取 '+follower.label+' 与单主 '+master.label+' 的只读跟单证据…';copyInspectorContent.replaceChildren();copyInspection=null;copyInspectionTab='edge';try{const query=new URLSearchParams();for(const key of ['platform','server','symbol','start','end'])if(params.get(key))query.set(key,params.get(key));if(follower.platform)query.set('platform',follower.platform);if(follower.server)query.set('server',follower.server);const response=await fetch('/api/accounts/by-login/'+encodeURIComponent(follower.label)+'/copy-origins?'+query.toString(),{cache:'no-store'});if(!response.ok)throw new Error('请求失败');const payload=await response.json(),origins=Array.isArray(payload.origins)?payload.origins:[],origin=origins.find(item=>String(item.account||'')===String(master.label)&&(!master.platform||!item.platform||item.platform===master.platform)&&(!master.server||!item.server||item.server===master.server))||origins.find(item=>String(item.account||'')===String(master.label));if(!origin)throw new Error('当前筛选范围没有找到该线条对应的单主订单证据');copyInspection={master,follower,origin,payload};copyInspectorTitle.textContent='跟随账户 '+follower.label+' → 单主 '+master.label;copyInspectorNote.textContent='数据按当前页面的平台、服务器、品种和时间筛选读取；不会写入交易或账户数据。';renderCopyInspection()}catch(error){copyInspectorContent.replaceChildren();const message=document.createElement('div');message.className='copy-inspector-error';message.textContent='无法读取跟单证据：'+(error.message||'请稍后重试');copyInspectorContent.append(message);copyInspectorNote.textContent='该弹窗不会影响关系图的继续扩散。'}}copyEdgeTab.addEventListener('click',()=>{copyInspectionTab='edge';renderCopyInspection()});copyMasterTab.addEventListener('click',()=>{copyInspectionTab='master';renderCopyInspection()});document.getElementById('copyInspectorClose').addEventListener('click',()=>copyInspector.classList.remove('open'));copyInspector.addEventListener('click',event=>{if(event.target===copyInspector)copyInspector.classList.remove('open')});window.addEventListener('keydown',event=>{if(event.key==='Escape')copyInspector.classList.remove('open')});const drawRelationEdgeWithCopyHit=drawRelationEdge;drawRelationEdge=function(edge){drawRelationEdgeWithCopyHit(edge);if(relationKey(edge.type)==='copy_order')copyHitEdges.push(edge)};const renderOverviewWithCopyHit=renderOverview;renderOverview=function(){copyHitEdges=[];renderOverviewWithCopyHit()};function distanceToCopyEdge(point,edge){const from=layout.get(edge.from.id),to=layout.get(edge.to.id);if(!from||!to)return Infinity;const dx=to.x-from.x,dy=to.y-from.y,lengthSquared=dx*dx+dy*dy;if(!lengthSquared)return Math.hypot(point.x-from.x,point.y-from.y);const ratio=Math.max(0,Math.min(1,((point.x-from.x)*dx+(point.y-from.y)*dy)/lengthSquared));return Math.hypot(point.x-(from.x+ratio*dx),point.y-(from.y+ratio*dy))}canvas.addEventListener('click',event=>{if(!data||copyInspector.classList.contains('open'))return;const point=world(event),nodeRadius=28/Math.max(view.scale,.1);if(graphNodes().some(node=>{const p=layout.get(node.id);return p&&Math.hypot(point.x-p.x,point.y-p.y)<nodeRadius}))return;let hit=null,distance=11/Math.max(view.scale,.1);for(const edge of copyHitEdges){const candidate=distanceToCopyEdge(point,edge);if(candidate<distance){hit=edge;distance=candidate}}if(hit)fetchCopyInspection(hit)});
const riskPanel=document.createElement('aside');riskPanel.className='risk-table';riskPanel.setAttribute('aria-label','风险表');riskPanel.innerHTML='<header><h2>风险表</h2><span id="riskTableNote">数据库状态为 T / TA / A 的账户</span></header><div class="risk-table-body" id="riskTable"></div>';const overviewLayout=document.createElement('div');overviewLayout.className='overview-layout';canvas.parentElement.before(overviewLayout);overviewLayout.append(riskPanel,canvas.parentElement);const riskTable=document.getElementById('riskTable'),riskTableNote=document.getElementById('riskTableNote');function riskStatusAccounts(){return accounts().filter(node=>['T','TA','A'].includes(databaseStatus(node))).sort((left,right)=>Number(right.score||0)-Number(left.score||0)||String(left.label).localeCompare(String(right.label),undefined,{numeric:true}))}function renderRiskTable(){if(!data||!riskTable)return;const rows=riskStatusAccounts();riskTable.replaceChildren();riskTableNote.textContent='数据库状态为 T / TA / A 的账户 · '+rows.length+' 个';if(!rows.length){const empty=document.createElement('div');empty.className='risk-empty';empty.textContent='当前图谱没有数据库状态为 T、TA 或 A 的账户。';riskTable.append(empty);return}for(const node of rows){const row=document.createElement('button');row.type='button';row.className='risk-row'+(node.id===selectedId?' active':'');const account=document.createElement('strong');account.textContent=node.label;const statusCell=document.createElement('span');const statusValue=databaseStatus(node);statusCell.className='risk-status risk-status-'+statusValue;statusCell.textContent=statusValue;const note=document.createElement('small');note.textContent='第 '+accountDepth(node)+' 层 · 传播分 '+Number(node.score||0).toFixed(1);row.append(account,statusCell,note);row.addEventListener('click',()=>{selectedId=node.id;activeGroupKey=layout.get(node.id)?.groupKey||'';activeType='';renderOverview();renderDetail();renderRiskTable()});riskTable.append(row)}}const renderOverviewWithRiskTable=renderOverview;renderOverview=function(){renderOverviewWithRiskTable();renderRiskTable()}
const originalRootChainEdges=rootChainEdges;rootChainEdges=function(nodes){const subject=accounts().find(item=>item.isSubject),edges=[],seen=new Set();if(!subject)return edges;for(const node of nodes){if(node.id===subject.id)continue;const chain=route(node);if(chain[0]?.id!==subject.id)continue;for(let index=1;index<chain.length;index++){const from=chain[index-1],to=chain[index],type=branchType(to),key=from.id+'|'+relationshipGroup(to);if(seen.has(key))continue;seen.add(key);edges.push({id:'root-group|'+key,from,to,type,directed:isDirectedRelation(type),grouped:true})}}return edges};
const originalRingLayout=ringLayout;ringLayout=function(all,w,h){const result=originalRingLayout(all,w,h),maxDepth=Math.max(1,...all.filter(node=>!node.isSubject).map(accountDepth).map(depth=>Math.min(4,depth)));return {...result,maxDepth}};
let selectedEdgeKey='',selectedEdgeNodes=new Set();const originalFetchCopyInspection=fetchCopyInspection;fetchCopyInspection=async edge=>{selectedEdgeKey=edge.id||'';selectedEdgeNodes=new Set([edge.source,edge.target]);renderOverview();return originalFetchCopyInspection(edge)};
// Do not draw a second straight overlay for a selected edge. The routed edge itself
// remains the sole relationship line; selection is represented by node halos and the
// evidence panel, avoiding misleading duplicate lines between the same endpoints.
const originalDrawRelationEdge=drawRelationEdge;drawRelationEdge=function(edge){originalDrawRelationEdge(edge)};
const originalRenderOverview=renderOverview;renderOverview=function(){originalRenderOverview();if(!data)return;const r=canvas.getBoundingClientRect(),placement=ringLayout(graphNodes(),r.width,r.height);ctx.save();ctx.translate(view.x,view.y);ctx.scale(view.scale,view.scale);for(let ring=1;ring<=4;ring++){const radius=placement.radii.get(ring);if(!radius)continue;const items=placement.rings.get(ring)||[],x=placement.center.x-radius-58,y=placement.center.y-radius+7;ctx.fillStyle='#101826';ctx.fillRect(x-4,y-14,150,20);if(items.length){const angle=-Math.PI*.75;ctx.fillStyle='#93acc6';ctx.font='11px Microsoft YaHei';ctx.textAlign='center';ctx.fillText('第 '+ring+' 圈 · '+items.length+' 个节点',placement.center.x+Math.cos(angle)*radius,placement.center.y+Math.sin(angle)*radius)}}for(const id of selectedEdgeNodes){const point=layout.get(id);if(point){ctx.strokeStyle='#fff';ctx.lineWidth=3;ctx.beginPath();ctx.arc(point.x,point.y,25,0,Math.PI*2);ctx.stroke()}}ctx.textAlign='start';ctx.restore()};
if(params.get('start'))rangeStart.value=params.get('start').replace(' ','T').slice(0,16);if(params.get('end'))rangeEnd.value=params.get('end').replace(' ','T').slice(0,16);apiUrl=function(value){if(!target)return '/api/kuzu-risk/graph?threshold='+value;const query=new URLSearchParams();for(const key of ['platform','server','symbol'])if(params.get(key))query.set(key,params.get(key));query.set('threshold',String(value));if(rangeStart.value)query.set('start',rangeStart.value.replace('T',' '));if(rangeEnd.value)query.set('end',rangeEnd.value.replace('T',' '));if(includeToxic.checked)query.set('include_toxic','true');return '/api/accounts/by-login/'+encodeURIComponent(target)+'/relationship-network?'+query.toString()};[rangeStart,rangeEnd].forEach(input=>input.addEventListener('change',()=>{clearTimeout(pollTimer);selectedId='';activeType='';selectedEdgeKey='';selectedEdgeNodes=new Set();load()}));
const originalRelationEdgeLabel=relationEdgeLabel;relationEdgeLabel=function(edge){if(edge.grouped){return originalRelationEdgeLabel(edge)+'（群组代表 '+(edge.groupCount||1)+' 个）'}return originalRelationEdgeLabel(edge)};const overviewWithGroupingNote=renderOverview;renderOverview=function(){overviewWithGroupingNote();const note=document.getElementById('overviewNote');if(note)note.textContent=note.textContent.replace('同一圈内按关系来源成簇','同一关系群组仅绘制一条代表线，成员仍在关系色块内展示；同一圈内按关系来源成簇')};
const previousSelectedBranch=selectedBranch;selectedBranch=function(node){const result=previousSelectedBranch(node),collapsed=new Map(),out=[];for(const edge of result.edges){const key=(edge.from.id||'')+'|'+edge.type;if(String(edge.id||'').startsWith('path|')||expandedRelationGroups.has(key)){out.push(edge);continue}if(!collapsed.has(key)){collapsed.set(key,{count:1});out.push({...edge,id:'selected-group|'+key,grouped:true})}else collapsed.get(key).count++}for(const edge of out)if(edge.grouped)edge.groupCount=collapsed.get((edge.from.id||'')+'|'+edge.type)?.count||1;return {...result,edges:out}};rangeStart.style.width='165px';rangeEnd.style.width='165px';
const detailBeforeGroups=renderDetail;renderDetail=function(){detailBeforeGroups();const node=byId().get(selectedId);if(!node||!groups)return;for(const button of groups.querySelectorAll('.group')){const toggle=document.createElement('small');toggle.className='group-toggle';toggle.textContent=expandedRelationGroups.has((node.id||'')+'|'+activeType)?'合并当前关系群组':'展开当前关系群组';toggle.title='点击后切换当前关系群组';button.append(toggle);toggle.addEventListener('click',event=>{event.stopPropagation();const key=(node.id||'')+'|'+activeType;if(expandedRelationGroups.has(key)){expandedRelationGroups.delete(key);toggle.textContent='展开当前关系群组'}else{expandedRelationGroups.add(key);toggle.textContent='合并当前关系群组'}renderOverview()})}};
const expandedRelationGroups=new Set();const groupMemberLabel='群组成员';const aggregationBaseSelectedBranch=ungroupedSelectedBranch;selectedBranch=function(node){const result=aggregationBaseSelectedBranch(node),out=[],seen=new Set();for(const edge of result.edges){const key=relationshipGroup(edge.to),path=String(edge.id||'').startsWith('path|');if(path||expandedRelationGroups.has(key)){out.push(edge);continue}if(seen.has(key))continue;seen.add(key);const count=result.edges.filter(item=>!String(item.id||'').startsWith('path|')&&relationshipGroup(item.to)===key).length;out.push({...edge,id:'selected-group|'+key,grouped:true,groupCount:count||1})}return {...result,edges:out}};rootChainEdges=function(nodes){const subject=accounts().find(item=>item.isSubject),out=[],seen=new Set();if(!subject)return out;for(const node of nodes){if(node.id===subject.id)continue;const chain=route(node);if(chain[0]?.id!==subject.id)continue;for(let index=1;index<chain.length;index++){const from=chain[index-1],to=chain[index],type=branchType(to),key=relationshipGroup(to),itemKey=from.id+'|'+to.id+'|'+type;if(expandedRelationGroups.has(key)){if(seen.has(itemKey))continue;seen.add(itemKey);out.push({id:'root|'+itemKey,from,to,type,directed:isDirectedRelation(type),groupCount:1});continue}if(seen.has(key))continue;seen.add(key);const count=nodes.filter(item=>parent(item)?.id===from.id&&relationshipGroup(item)===key).length;out.push({id:'root-group|'+key,from,to,type,directed:isDirectedRelation(type),grouped:true,groupCount:count||1})}}return out};const aggregationDetailBase=renderDetail;renderDetail=function(){aggregationDetailBase();const node=byId().get(selectedId);if(!node||!groups)return;const types=typeGroups(node);for(const [index,card] of [...groups.querySelectorAll('.group')].entries()){card.querySelectorAll('.group-toggle').forEach(item=>item.remove());const type=types[index]||branchType(node),member=graphNodes().find(item=>relationshipGroup(item)===(node.id+'|'+type)),key=member?relationshipGroup(member):relationshipGroup(node),toggle=document.createElement('small');toggle.className='group-toggle';toggle.textContent=expandedRelationGroups.has(key)?'合并当前关系群组':'展开当前关系群组';toggle.title='只展开或合并这一组关系成员，不影响其他群组';card.append(toggle);toggle.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();if(expandedRelationGroups.has(key))expandedRelationGroups.delete(key);else expandedRelationGroups.add(key);renderOverview();renderDetail()})}};
const aggregationBaseSelectedBranch2=previousSelectedBranch;selectedBranch=function(node){const result=aggregationBaseSelectedBranch2(node),out=[],seen=new Set();for(const edge of result.edges){const key=relationshipGroup(edge.to),path=String(edge.id||'').startsWith('path|');if(path||expandedRelationGroups.has(key)){out.push(edge);continue}if(seen.has(key))continue;seen.add(key);const count=result.edges.filter(item=>!String(item.id||'').startsWith('path|')&&relationshipGroup(item.to)===key).length;out.push({...edge,id:'selected-group|'+key,grouped:true,groupCount:count||1})}return {...result,edges:out}};function angleBetween(angle,start,end){const norm=value=>(value+Math.PI*2)%(Math.PI*2),a=norm(angle),s=norm(start),e=norm(end);return s<=e?a>=s&&a<=e:a>=s||a<=e}function nodeHitPixels(node,scale){const depth=Math.min(4,accountDepth(node)),size=node.isSubject?19:node.type==='ib_user'?11:depth===1?10.5:depth===2?8.5:7;return Math.max(7,Math.min(22,size*Math.max(scale,.01)+4))}canvas.addEventListener('click',event=>{if(!data)return;const rect=canvas.getBoundingClientRect(),mouse={x:event.clientX-rect.left,y:event.clientY-rect.top},scale=Math.max(view.scale,.01);for(const node of graphNodes()){const p=layout.get(node.id);if(!p)continue;const sx=view.x+p.x*scale,sy=view.y+p.y*scale;if(Math.hypot(mouse.x-sx,mouse.y-sy)<=nodeHitPixels(node,scale))return}const placement=ringLayout(graphNodes(),rect.width,rect.height),center={x:view.x+placement.center.x*scale,y:view.y+placement.center.y*scale},dx=mouse.x-center.x,dy=mouse.y-center.y,radius=Math.hypot(dx,dy),angle=Math.atan2(dy,dx);let bandHit=null,bestBandDistance=26;for(const depth of [1,2,3,4])for(const group of placement.groupsByDepth.get(depth)||[]){if(group.nodes.length<2&&!expandedRelationGroups.has(group.key))continue;if(!angleBetween(angle,group.start,group.end))continue;const screenDistance=Math.abs(radius-group.radius*scale);if(screenDistance>=bestBandDistance)continue;bestBandDistance=screenDistance;bandHit=group}if(!bandHit)return;event.preventDefault();event.stopImmediatePropagation();if(expandedRelationGroups.has(bandHit.key))expandedRelationGroups.delete(bandHit.key);else expandedRelationGroups.add(bandHit.key);activeGroupKey=bandHit.key;renderOverview();renderDetail()},true);
const detailToggleFixBase=renderDetail;renderDetail=function(){detailToggleFixBase();const node=byId().get(selectedId);if(!node||!groups)return;const types=typeGroups(node);for(const [index,button] of [...groups.querySelectorAll('.group')].entries()){button.querySelectorAll('.group-toggle').forEach(item=>item.remove());const type=types[index]||branchType(node),edge=(data.relationships||[]).find(item=>item.type===type&&(item.source===node.id||item.target===node.id)),target=edge?byId().get(edge.source===node.id?edge.target:edge.source):null,key=target?relationshipGroup(target):relationshipGroup(node),toggle=document.createElement('small');toggle.className='group-toggle';toggle.textContent=expandedRelationGroups.has(key)?'合并当前关系群组':'展开当前关系群组';toggle.title='只展开或合并这一组关系成员，不影响其他群组';button.append(toggle);toggle.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();if(expandedRelationGroups.has(key))expandedRelationGroups.delete(key);else expandedRelationGroups.add(key);activeGroupKey=key;renderOverview();renderDetail()})}};
// Final relation interaction/aggregation layer. Keep a canonical key per source account and relation family.
function edgeCommunityKey(edge){const source=edge?.from?.id??edge?.source??'';return String(source)+'|'+relationKey(edge?.type||'unknown')}
function edgeIsPath(edge){return String(edge?.id||'').startsWith('path|')}
function edgeGroupIsExpanded(edge,anchor){return expandedRelationGroups.has(edgeCommunityKey(edge))||expandedRelationGroups.has(String(anchor||'')+'|'+relationKey(edge?.type||'unknown'))}
const finalBaseSelectedBranch=ungroupedSelectedBranch;
selectedBranch=function(node){const result=finalBaseSelectedBranch(node),allEdges=result.edges||[],counts=new Map(),out=[],seen=new Set();for(const edge of allEdges){if(edgeIsPath(edge))continue;const key=edgeCommunityKey(edge);counts.set(key,(counts.get(key)||0)+1)}for(const edge of allEdges){const key=edgeIsPath(edge)?edge.id:edgeCommunityKey(edge);if(edgeIsPath(edge)||edgeGroupIsExpanded(edge,node?.id)){out.push(edge);continue}if(seen.has(key))continue;seen.add(key);out.push({...edge,id:'selected-group|'+key,grouped:true,groupCount:counts.get(key)||1})}return {...result,edges:out}};
rootChainEdges=function(nodes){const subject=accounts().find(item=>item.isSubject),out=[],counts=new Map(),seen=new Set();if(!subject)return out;const routes=[];for(const node of nodes){if(node.id===subject.id)continue;const chain=route(node);if(chain[0]?.id!==subject.id)continue;for(let index=1;index<chain.length;index++){const from=chain[index-1],to=chain[index],type=branchType(to),edge={from,to,type,directed:isDirectedRelation(type)},key=edgeCommunityKey(edge);routes.push({...edge,key,itemKey:from.id+'|'+to.id+'|'+type});counts.set(key,(counts.get(key)||0)+1)}}for(const edge of routes){if(edgeGroupIsExpanded(edge,edge.from.id)){if(seen.has(edge.itemKey))continue;seen.add(edge.itemKey);out.push({...edge,id:'root|'+edge.itemKey,groupCount:1});continue}if(seen.has(edge.key))continue;seen.add(edge.key);out.push({id:'root-group|'+edge.key,from:edge.from,to:edge.to,type:edge.type,directed:edge.directed,grouped:true,groupCount:counts.get(edge.key)||1})}return out};
const finalDrawRelationEdgeBase=drawRelationEdge,relationHitEdges=[];
drawRelationEdge=function(edge){finalDrawRelationEdgeBase(edge);relationHitEdges.push(edge)};
const finalRenderOverviewBase=renderOverview;
renderOverview=function(){relationHitEdges.length=0;finalRenderOverviewBase()};
function distanceToRelationEdge(point,edge){const from=layout.get(edge?.from?.id),to=layout.get(edge?.to?.id);if(!from||!to)return Infinity;const dx=to.x-from.x,dy=to.y-from.y,len=dx*dx+dy*dy;if(!len)return Math.hypot(point.x-from.x,point.y-from.y);const ratio=Math.max(0,Math.min(1,((point.x-from.x)*dx+(point.y-from.y)*dy)/len));return Math.hypot(point.x-(from.x+ratio*dx),point.y-(from.y+ratio*dy))}
canvas.addEventListener('click',event=>{if(!data||copyInspector.classList.contains('open'))return;const point=world(event),nodeRadius=30/Math.max(view.scale,.1);if(graphNodes().some(node=>{const p=layout.get(node.id);return p&&Math.hypot(point.x-p.x,point.y-p.y)<nodeRadius}))return;let hit=null,best=16/Math.max(view.scale,.1);for(const edge of relationHitEdges){const distance=distanceToRelationEdge(point,edge);if(distance<best){best=distance;hit=edge}}if(!hit)return;selectedEdgeKey=hit.id;activeGroupKey=edgeCommunityKey(hit);event.preventDefault();event.stopImmediatePropagation();renderOverview();renderDetail()},true);
const finalRenderDetailBase=renderDetail;
renderDetail=function(){finalRenderDetailBase();const node=byId().get(selectedId);if(!node||!groups)return;const types=typeGroups(node);for(const [index,card] of [...groups.querySelectorAll('.group')].entries()){card.querySelectorAll('.group-toggle').forEach(item=>item.remove());const type=types[index]||branchType(node),key=String(node.id)+'|'+relationKey(type),toggle=document.createElement('small');toggle.className='group-toggle';toggle.textContent=expandedRelationGroups.has(key)?'合并当前关系群落':'展开当前关系群落';toggle.title='点击后展开或合并该账户发出的同类关系成员';card.append(toggle);toggle.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();if(expandedRelationGroups.has(key))expandedRelationGroups.delete(key);else expandedRelationGroups.add(key);activeGroupKey=key;renderOverview();renderDetail()})}};
// A collapsed community is a real canvas object: member nodes share an anchor and common edges end there.
relationshipGroup=function(node){if(node.isSubject)return'subject';const owner=parent(node);return String(owner?owner.id:'root')+'|'+relationKey(branchType(node))};
function groupAnchorPoint(group,center){const points=group.nodes.map(node=>layout.get(node.id)).filter(Boolean);if(!points.length)return{x:center.x,y:center.y};let vx=0,vy=0,radius=0;for(const point of points){const dx=point.x-center.x,dy=point.y-center.y,length=Math.hypot(dx,dy)||1;vx+=dx/length;vy+=dy/length;radius+=length}const angle=Math.atan2(vy,vx),r=radius/points.length;return{x:center.x+Math.cos(angle)*r,y:center.y+Math.sin(angle)*r}}
let collapsedVisualGroups=[];
const communityRingLayoutBase=ringLayout;
ringLayout=function(all,w,h){const result=communityRingLayoutBase(all,w,h);collapsedVisualGroups=[];for(const depth of [1,2,3,4])for(const group of result.groupsByDepth.get(depth)||[]){if(group.nodes.length<2||expandedRelationGroups.has(group.key))continue;const anchor=groupAnchorPoint(group,result.center),groupMembers=[...group.nodes];for(const member of groupMembers){const point=layout.get(member.id);if(point)layout.set(member.id,{...point,x:anchor.x,y:anchor.y,groupKey:group.key})}collapsedVisualGroups.push({...group,anchor,groupMembers})}return result};
function groupedEdges(result,node){const allEdges=result.edges||[],buckets=new Map(),out=[];for(const edge of allEdges){if(edgeIsPath(edge))continue;const key=edgeCommunityKey(edge);if(!buckets.has(key))buckets.set(key,[]);buckets.get(key).push(edge)}for(const edge of allEdges){if(edgeIsPath(edge)||edgeGroupIsExpanded(edge,node?.id)){out.push(edge);continue}const key=edgeCommunityKey(edge),groupMembers=buckets.get(key)||[];if(groupMembers[0]!==edge)continue;out.push({...edge,id:'selected-group|'+key,grouped:true,groupCount:groupMembers.length,groupMembers:groupMembers.map(item=>item.to)})}return{...result,edges:out}}
selectedBranch=function(node){return groupedEdges(finalBaseSelectedBranch(node),node)};
function drawCollapsedCommunities(){if(!data||!collapsedVisualGroups.length)return;ctx.save();ctx.translate(view.x,view.y);ctx.scale(view.scale,view.scale);for(const group of collapsedVisualGroups){const theme=relationTheme(group.type),active=group.key===activeGroupKey,size=18;ctx.beginPath();ctx.arc(group.anchor.x,group.anchor.y,size,0,Math.PI*2);ctx.fillStyle='rgba(8,20,36,.96)';ctx.fill();ctx.strokeStyle=active?'#f8fafc':theme.stroke;ctx.lineWidth=active?4:3;ctx.stroke();ctx.beginPath();ctx.arc(group.anchor.x,group.anchor.y,size-5,0,Math.PI*2);ctx.fillStyle=theme.fill;ctx.fill();ctx.fillStyle='#fff';ctx.font='bold 11px Microsoft YaHei';ctx.textAlign='center';ctx.fillText(String(group.groupMembers.length),group.anchor.x,group.anchor.y+4);ctx.font='bold 10px Microsoft YaHei';ctx.fillStyle=theme.stroke;ctx.fillText(relationShortLabel(group.type)+' 群落',group.anchor.x,group.anchor.y-25)}ctx.textAlign='start';ctx.restore()}
const communityRenderOverviewBase=renderOverview;
renderOverview=function(){communityRenderOverviewBase();drawCollapsedCommunities()};
canvas.addEventListener('click',event=>{if(!data)return;const point=world(event),radius=24/Math.max(view.scale,.1);for(const group of collapsedVisualGroups){if(Math.hypot(point.x-group.anchor.x,point.y-group.anchor.y)>radius)continue;event.preventDefault();event.stopImmediatePropagation();expandedRelationGroups.add(group.key);activeGroupKey=group.key;renderOverview();renderDetail();return}},true);
// Expanded member nodes take precedence over a community-band hit target.
canvas.addEventListener('click',event=>{if(!data)return;const rect=canvas.getBoundingClientRect(),mouse={x:event.clientX-rect.left,y:event.clientY-rect.top},scale=Math.max(view.scale,.01);for(const node of graphNodes()){const p=layout.get(node.id);if(!p)continue;const sx=view.x+p.x*scale,sy=view.y+p.y*scale;if(Math.hypot(mouse.x-sx,mouse.y-sy)>nodeHitPixels(node,scale))continue;selectedId=node.id;activeGroupKey=p.groupKey||'';activeType='';event.preventDefault();event.stopImmediatePropagation();renderOverview();renderDetail();return}},true);
// Route close-angle edges through separate curved lanes and use the same geometry for hit testing.
let routeLaneCursor=new Map();
function edgeLaneKey(edge){return String(edge?.from?.id||'')}
function relationRoute(edge){const from=layout.get(edge?.from?.id),to=layout.get(edge?.to?.id);if(!from||!to)return null;const dx=to.x-from.x,dy=to.y-from.y,length=Math.hypot(dx,dy)||1;let lane=edge.__routeLane;if(lane===undefined){const key=edgeLaneKey(edge),next=routeLaneCursor.get(key)||0;lane=next;routeLaneCursor.set(key,next+1);edge.__routeLane=lane}const hash=String(edge?.id||edge?.type||'').split('').reduce((sum,char)=>(sum*31+char.charCodeAt(0))>>>0,7),baseSide=hash%2?1:-1,side=lane===0?baseSide:lane%2?-(lane+1)/2:lane/2,separation=Math.min(92,Math.max(26,length*.11)),offset=side*separation,nx=-dy/length,ny=dx/length,mx=(from.x+to.x)/2,my=(from.y+to.y)/2;return{from,to,control:{x:mx+nx*offset,y:my+ny*offset},length}}
function quadraticPoint(route,t){const u=1-t,a=route.from,b=route.control,c=route.to;return{x:u*u*a.x+2*u*t*b.x+t*t*c.x,y:u*u*a.y+2*u*t*b.y+t*t*c.y}}
function drawRoutedRelationEdge(edge){const route=relationRoute(edge);if(!route||route.length<18)return;const{from,to,control}=route,theme=relationTheme(edge.type);ctx.save();ctx.strokeStyle=theme.stroke;ctx.globalAlpha=.92;ctx.lineWidth=2.4;ctx.beginPath();ctx.moveTo(from.x,from.y);ctx.quadraticCurveTo(control.x,control.y,to.x,to.y);ctx.stroke();const angle=Math.atan2(to.y-control.y,to.x-control.x);if(edge.directed){const tip=Math.max(7,Math.min(14,route.length*.14)),backX=to.x-Math.cos(angle)*tip,backY=to.y-Math.sin(angle)*tip;ctx.globalAlpha=1;ctx.fillStyle=theme.stroke;ctx.beginPath();ctx.moveTo(to.x,to.y);ctx.lineTo(backX+Math.cos(angle+2.55)*6,backY+Math.sin(angle+2.55)*6);ctx.lineTo(backX+Math.cos(angle-2.55)*6,backY+Math.sin(angle-2.55)*6);ctx.closePath();ctx.fill()}const label=relationEdgeLabel(edge),middle=quadraticPoint(route,.5),tangent={x:2*(1-.5)*(control.x-from.x)+2*.5*(to.x-control.x),y:2*(1-.5)*(control.y-from.y)+2*.5*(to.y-control.y)},labelAngle=Math.atan2(tangent.y,tangent.x),lx=middle.x-Math.sin(labelAngle)*Math.min(18,Math.max(9,route.length*.12)),ly=middle.y+Math.cos(labelAngle)*Math.min(18,Math.max(9,route.length*.12));ctx.globalAlpha=1;ctx.font='bold 10px Microsoft YaHei';const width=ctx.measureText(label).width+10;ctx.fillStyle='rgba(11,18,32,.9)';ctx.fillRect(lx-width/2,ly-8,width,16);ctx.fillStyle=theme.stroke;ctx.textAlign='center';ctx.fillText(label,lx,ly+3);ctx.restore();ctx.textAlign='start'}
// Route repair: scoreLedger.primary can point to a same-CRM sibling in both
// directions. Following that single ledger entry makes parent() cycle and
// silently removes the node from graphNodes(), so a selected node loses its
// route back to the subject. Resolve ancestry from the bounded evidence graph
// with a deterministic BFS instead; ledger entries are only tie-breakers.
const galaxyLegacyParent=parent,galaxyLegacyRoute=route;
let galaxyRouteCacheData=null,galaxyRouteCacheKey='',galaxyRouteCache=new Map(),galaxyRoutePredecessor=new Map(),galaxyRoutePredecessorEdge=new Map(),galaxyRouteAdjacency=new Map(),galaxyRouteStepTypes=new Map();
function galaxyBuildRouteCache(){
  if(!data)return;
  if(!data.__galaxyTopologyKey)data.__galaxyTopologyKey=[...(data.entities||[]).filter(item=>item.type==='account'||item.type==='ib_user').map(item=>item.id),...(data.relationships||[]).map(edge=>[edge.type,edge.source,edge.target].join('|'))].sort().join('||');
  if(galaxyRouteCacheData===data||galaxyRouteCacheKey===data.__galaxyTopologyKey)return;
  galaxyRouteCacheData=data;galaxyRouteCacheKey=data.__galaxyTopologyKey;galaxyRouteCache=new Map();galaxyRoutePredecessor=new Map();galaxyRoutePredecessorEdge=new Map();galaxyRouteAdjacency=new Map();galaxyRouteStepTypes=new Map();
  const subject=accounts().find(item=>item.isSubject),map=byId();
  if(!subject)return;
  const allowed=new Set([...map.values()].map(item=>item.type)),queue=[subject.id];
  galaxyRoutePredecessor.set(subject.id,null);
  for(const edge of data.relationships||[]){
    const source=map.get(edge.source),target=map.get(edge.target);
    if(!source||!target||!allowed.has(source.type)||!allowed.has(target.type))continue;
    if(!galaxyRouteAdjacency.has(source.id))galaxyRouteAdjacency.set(source.id,[]);
    if(!galaxyRouteAdjacency.has(target.id))galaxyRouteAdjacency.set(target.id,[]);
    galaxyRouteAdjacency.get(source.id).push({node:target,type:edge.type});
    galaxyRouteAdjacency.get(target.id).push({node:source,type:edge.type});
  }
  while(queue.length){
    const currentId=queue.shift(),current=map.get(currentId),neighbors=new Map();
    for(const candidate of galaxyRouteAdjacency.get(currentId)||[]){
      const other=candidate?.node;
      if(other&&!galaxyRoutePredecessor.has(other.id))neighbors.set(other.id,candidate);
    }
    for(const ledger of current?.scoreLedger||[]){
      const other=map.get(ledger.from);
      if(other&&allowed.has(other.type)&&!galaxyRoutePredecessor.has(other.id))neighbors.set(other.id,{node:other,type:ledger.type||'unknown'});
    }
    const ordered=[...neighbors.values()].sort((left,right)=>{
      const leftNode=left.node,rightNode=right.node;
      const leftScore=Math.max(...(current?.scoreLedger||[]).filter(item=>item.from===leftNode.id).map(item=>Number(item.contribution)||0),0);
      const rightScore=Math.max(...(current?.scoreLedger||[]).filter(item=>item.from===rightNode.id).map(item=>Number(item.contribution)||0),0);
      return rightScore-leftScore||String(leftNode.id).localeCompare(String(rightNode.id));
    });
    for(const candidate of ordered){const other=candidate.node;galaxyRoutePredecessor.set(other.id,currentId);galaxyRoutePredecessorEdge.set(other.id,candidate.type||'unknown');queue.push(other.id)}
  }
  for(const id of galaxyRoutePredecessor.keys()){
    const fullPath=[],seen=new Set();let cursor=id;
    while(cursor&&!seen.has(cursor)){seen.add(cursor);fullPath.unshift(map.get(cursor));cursor=galaxyRoutePredecessor.get(cursor)}
    const visible=fullPath.filter(item=>item&&(item.type==='account'||item.type==='ib_user'));
    for(let index=1;index<visible.length;index++){
      const fromIndex=fullPath.findIndex(item=>item?.id===visible[index-1]?.id),toIndex=fullPath.findIndex(item=>item?.id===visible[index]?.id);
      let stepType='unknown';
      for(let step=fromIndex+1;step<=toIndex;step++)stepType=galaxyRoutePredecessorEdge.get(fullPath[step]?.id)||stepType;
      galaxyRouteStepTypes.set(visible[index-1].id+'|'+visible[index].id,stepType);
    }
    galaxyRouteCache.set(id,visible);
  }
}
function galaxyAccountRoute(node){
  galaxyBuildRouteCache();
  return node?galaxyRouteCache.get(node.id)||[]:[];
}
parent=function(node){const path=galaxyAccountRoute(node);return path.length>1?path[path.length-2]:null};
route=function(node){return galaxyAccountRoute(node)};
const galaxyLegacyBranchType=branchType;
branchType=function(node){
  if(!node||node.isSubject)return'unknown';
  const path=galaxyAccountRoute(node),from=path.length>1?path[path.length-2]:null;
  if(from){
    const ledger=(node.scoreLedger||[]).find(item=>item.from===from.id),routeType=galaxyRouteStepTypes.get(from.id+'|'+node.id);
    if(routeType&&routeType!=='unknown')return routeType;
    if(ledger?.type)return ledger.type;
    const edge=(data.relationships||[]).find(item=>(item.source===from.id&&item.target===node.id)||(item.target===from.id&&item.source===node.id));
    if(edge?.type)return edge.type;
  }
  return galaxyLegacyBranchType(node);
};
// Render-boundary deduplication: collapsed members share one visible group
// anchor, so deduplicate by the visible endpoint rather than the hidden member
// account id. Different relation families remain distinct evidence.
function galaxyVisualEndpointKey(node){
  const id=String(node?.id||node||'');
  const point=layout.get(id);
  return point?.groupKey?'group:'+point.groupKey:id;
}
function galaxyRenderEdgeKey(edge){
  const from=galaxyVisualEndpointKey(edge?.from)||String(edge?.source||''),to=galaxyVisualEndpointKey(edge?.to)||String(edge?.target||''),type=relationKey(edge?.type||'unknown');
  return isDirectedRelation(edge?.type)?from+'>'+to+'|'+type:[from,to].sort().join('|')+'|'+type;
}
function galaxyFocusEdgeVisible(edge){
  const selected=byId().get(selectedId),subject=accounts().find(item=>item.isSubject);
  if(!selected||!subject||selected.id===subject.id)return true;
  const chain=route(selected),pairs=new Set(chain.slice(1).map((node,index)=>[chain[index].id,node.id].sort().join('|')));
  return pairs.has([edge?.from?.id,edge?.to?.id].sort().join('|'))||edge?.from?.id===selected.id||edge?.to?.id===selected.id;
}
const routedRenderOverviewBase=renderOverview;
let galaxyRenderedEdgeKeys=new Set();
drawRelationEdge=function(edge){
  if(!galaxyFocusEdgeVisible(edge))return;
  const key=galaxyRenderEdgeKey(edge);
  if(galaxyRenderedEdgeKeys.has(key))return;
  galaxyRenderedEdgeKeys.add(key);
  drawRoutedRelationEdge(edge);
  relationHitEdges.push(edge);
  if(relationKey(edge.type)==='copy_order')copyHitEdges.push(edge)
};
function galaxyDrawSelectedRoute(){
  const selected=byId().get(selectedId),subject=accounts().find(item=>item.isSubject);
  if(!selected||!subject||selected.id===subject.id)return;
  const chain=galaxyAccountRoute(selected);
  if(chain.length<2||chain[0]?.id!==subject.id)return;
  for(let index=1;index<chain.length;index++){
    const from=chain[index-1],to=chain[index],type=branchType(to);
    // Draw the audited account/IB path explicitly after all aggregate layers.
    // The render-boundary key still collapses it if the same logical edge was
    // already emitted by a community/root-chain renderer.
    drawRelationEdge({id:'focus-route|'+from.id+'|'+to.id+'|'+type,from,to,type,directed:isDirectedRelation(type),focusRoute:true});
  }
}
renderOverview=function(){routeLaneCursor.clear();galaxyRenderedEdgeKeys.clear();routedRenderOverviewBase();galaxyDrawSelectedRoute();const selectedNode=byId().get(selectedId),subject=accounts().find(item=>item.isSubject),note=document.getElementById('overviewNote');if(note&&selectedNode&&subject&&selectedNode.id!==subject.id)note.textContent='局部调查视图：仅显示「'+selectedNode.label+'」的临近关系及其返回中心账户的完整路径；点击空白处恢复全部关系。';const selectedEdge=relationHitEdges.find(edge=>edge.id===selectedEdgeKey);if(selectedEdge&&relationKey(selectedEdge.type)==='copy_order'&&!copyInspector.classList.contains('open'))fetchCopyInspection(selectedEdge)};
// Blank-canvas clicks intentionally have no reset behavior. The explicit
// "恢复初始" control at the end of this script is the only reset path.
distanceToRelationEdge=function(point,edge){const route=relationRoute(edge);if(!route)return Infinity;let best=Infinity,previous=route.from;for(let index=1;index<=24;index++){const current=quadraticPoint(route,index/24),dx=current.x-previous.x,dy=current.y-previous.y,lengthSquared=dx*dx+dy*dy;let ratio=lengthSquared?Math.max(0,Math.min(1,((point.x-previous.x)*dx+(point.y-previous.y)*dy)/lengthSquared)):0;best=Math.min(best,Math.hypot(point.x-(previous.x+ratio*dx),point.y-(previous.y+ratio*dy)));previous=current}return best};
// Keep the galaxy drawing unchanged; only reorganize the surrounding workspace like the focus-force page.
const galaxyShellStyle=document.createElement('style');galaxyShellStyle.textContent='.stage{max-width:none;padding:12px 16px}.galaxy-workspace{height:calc(100vh - 142px);display:grid;grid-template-columns:250px minmax(520px,1fr) 330px;gap:12px;min-height:560px}.galaxy-panel{border:1px solid #24496e;background:#08182b;border-radius:12px;overflow:hidden;min-width:0;display:flex;flex-direction:column}.galaxy-panel-head{height:48px;padding:13px 15px;border-bottom:1px solid #204466;font-weight:700}.galaxy-panel-head span{float:right;color:#7ea6d1;font-size:12px;font-weight:400}.galaxy-locator{width:100%;height:270px;display:block;background:#071426}.galaxy-locator-note{padding:10px 13px;color:#8eacd0;font-size:12px;line-height:1.5;border-top:1px solid #204466}.galaxy-center{min-width:0;display:flex;flex-direction:column}.galaxy-center .board{height:auto;min-height:0;flex:1;border:0;border-radius:0}.galaxy-center .overview-note,.galaxy-center .group-legend{padding-left:13px;padding-right:13px}.galaxy-side{padding:14px;overflow:auto}.galaxy-side .caption{padding:0 0 10px}.galaxy-side .detail{border-top:1px solid #334155;padding-top:12px}.galaxy-side .flow{grid-template-columns:1fr;gap:12px}.galaxy-side .subject:after{display:none}@media(max-width:1100px){.galaxy-workspace{grid-template-columns:205px minmax(420px,1fr) 270px}}@media(max-width:820px){body{overflow:auto}.galaxy-workspace{height:auto;min-height:calc(100vh - 142px);grid-template-columns:1fr}.galaxy-locator{height:220px}.galaxy-center{height:620px}.galaxy-side{min-height:300px}}';document.head.append(galaxyShellStyle);
const galaxyBoard=document.querySelector('.board'),galaxyOverview=galaxyBoard&&galaxyBoard.parentElement,galaxyRisk=document.querySelector('.risk-table'),galaxyNote=document.getElementById('overviewNote'),galaxyLegend=document.querySelector('.group-legend'),galaxyCaption=document.querySelector('.caption'),galaxyDetail=document.querySelector('.detail');if(galaxyBoard&&galaxyOverview){const workspace=document.createElement('div');workspace.className='galaxy-workspace';const left=document.createElement('section');left.className='galaxy-panel';left.innerHTML='<div class="galaxy-panel-head">全局定位图 <span id="galaxyLocatorCount">加载中</span></div><canvas class="galaxy-locator" id="galaxyLocator" aria-label="星系图全局定位图"></canvas><div class="galaxy-locator-note">始终显示全部账户，不受右侧群落展开或合并影响；颜色按 B→M→P→T→A→TA 逐级加深，点击账户可查看其到中心账户的完整路径。</div>';const center=document.createElement('section');center.className='galaxy-panel galaxy-center';center.innerHTML='<div class="galaxy-panel-head">星系图详细关系 <span id="galaxyGalaxyMeta">按扩散圈层显示</span></div>';const side=document.createElement('aside');side.className='galaxy-panel galaxy-side';if(galaxyCaption)side.append(galaxyCaption);if(galaxyDetail)side.append(galaxyDetail);center.append(galaxyBoard);if(galaxyNote)center.append(galaxyNote);if(galaxyLegend)center.append(galaxyLegend);if(galaxyRisk)left.append(galaxyRisk);workspace.append(left,center,side);galaxyOverview.parentElement.insertBefore(workspace,galaxyOverview);galaxyOverview.remove();
const locatorCanvas=document.getElementById('galaxyLocator'),locatorCtx=locatorCanvas.getContext('2d');let galaxyLocatorHitNodes=[];
function galaxyLocatorNodes(){return accounts().slice().sort((left,right)=>accountDepth(left)-accountDepth(right)||String(left.login||left.id).localeCompare(String(right.login||right.id),undefined,{numeric:true}))}
function galaxyLocatorStatusColor(node){const palette={B:'#64748b',M:'#f59e0b',P:'#f97316',T:'#ef4444',A:'#dc2626',TA:'#991b1b'};return palette[databaseStatus(node)]||palette.B}
function galaxyLocatorLayout(width,height,nodes=galaxyLocatorNodes()){const positions=new Map(),subject=nodes.find(node=>node.isSubject),cx=width/2,cy=height/2,groups=new Map();if(subject)positions.set(subject.id,{x:cx,y:cy,node:subject,depth:0});for(const node of nodes){if(node.isSubject)continue;const depth=Math.max(1,accountDepth(node));if(!groups.has(depth))groups.set(depth,[]);groups.get(depth).push(node)}const depths=[...groups.keys()].sort((a,b)=>a-b),outer=Math.max(24,Math.min(width,height)*.43),step=outer/Math.max(1,depths.length),radii=[];depths.forEach((depth,ringIndex)=>{const members=groups.get(depth),radius=step*(ringIndex+1),offset=-Math.PI/2+(ringIndex%2)*Math.PI/Math.max(1,members.length);radii.push(radius);members.forEach((node,index)=>{const angle=offset+Math.PI*2*index/Math.max(1,members.length);positions.set(node.id,{x:cx+Math.cos(angle)*radius,y:cy+Math.sin(angle)*radius,node,depth})})});return{positions,cx,cy,radii}}
function galaxyLocatorAccountPath(node,positions){return route(node).filter(item=>positions.has(item.id))}
function galaxyLocatorDrawPath(path,positions,colorValue,lineWidth){if(path.length<2)return;locatorCtx.beginPath();path.forEach((node,index)=>{const point=positions.get(node.id);if(index===0)locatorCtx.moveTo(point.x,point.y);else locatorCtx.lineTo(point.x,point.y)});locatorCtx.strokeStyle=colorValue;locatorCtx.lineWidth=lineWidth;locatorCtx.stroke()}
function renderGalaxyLocator(){if(!data||!locatorCanvas)return;const rect=locatorCanvas.getBoundingClientRect(),d=devicePixelRatio||1,w=Math.max(1,rect.width),h=Math.max(1,rect.height),nodes=galaxyLocatorNodes(),projection=galaxyLocatorLayout(w,h,nodes),positions=projection.positions;locatorCanvas.width=Math.round(w*d);locatorCanvas.height=Math.round(h*d);locatorCtx.setTransform(d,0,0,d,0,0);locatorCtx.fillStyle='#071426';locatorCtx.fillRect(0,0,w,h);locatorCtx.strokeStyle='#315c85';locatorCtx.lineWidth=1;locatorCtx.setLineDash([4,7]);for(const radius of projection.radii){locatorCtx.beginPath();locatorCtx.arc(projection.cx,projection.cy,radius,0,Math.PI*2);locatorCtx.stroke()}locatorCtx.setLineDash([]);for(const node of nodes){if(node.isSubject)continue;galaxyLocatorDrawPath(galaxyLocatorAccountPath(node,positions),positions,'rgba(71,129,178,.22)',1)}const selected=nodes.find(node=>node.id===selectedId);if(selected)galaxyLocatorDrawPath(galaxyLocatorAccountPath(selected,positions),positions,'#54c7ff',2.5);galaxyLocatorHitNodes=[];for(const node of nodes){const point=positions.get(node.id);if(!point)continue;const radius=node.isSubject?7:4.5;locatorCtx.beginPath();locatorCtx.arc(point.x,point.y,radius,0,Math.PI*2);locatorCtx.fillStyle=galaxyLocatorStatusColor(node);locatorCtx.fill();if(node.isSubject||node.id===selectedId){locatorCtx.strokeStyle='#fff';locatorCtx.lineWidth=node.isSubject?2.5:2;locatorCtx.stroke()}galaxyLocatorHitNodes.push({id:node.id,x:point.x,y:point.y,radius:Math.max(11,radius+5)})}const count=document.getElementById('galaxyLocatorCount');if(count)count.textContent=nodes.length+'个账户 · 全部展开'}
function galaxyDispatchLocatorClick(event){if(!data||!galaxyLocatorHitNodes.length)return;const rect=locatorCanvas.getBoundingClientRect(),x=event.clientX-rect.left,y=event.clientY-rect.top;let hit=null,distance=Infinity;for(const candidate of galaxyLocatorHitNodes){const current=Math.hypot(x-candidate.x,y-candidate.y);if(current<=candidate.radius&&current<distance){hit=candidate;distance=current}}if(!hit)return;event.preventDefault();event.stopImmediatePropagation();selectedId=hit.id;activeGroupKey='';activeType='';selectedEdgeKey='';selectedEdgeNodes=new Set();renderOverview();renderDetail()}
locatorCanvas.addEventListener('click',galaxyDispatchLocatorClick,true);const galaxyRenderWithLocator=renderOverview;renderOverview=function(){galaxyRenderWithLocator();renderGalaxyLocator()};window.addEventListener('resize',renderGalaxyLocator);}
// Galaxy compatibility fixes: retain cumulative snapshots and never hide returned nodes whose path is pending.
const galaxyOriginalRelationTheme=relationTheme,galaxyOriginalRelationShortLabel=relationShortLabel,galaxyOriginalRelationEdgeLabel=relationEdgeLabel;
relationTheme=function(type){if(type==='unresolved_path')return{stroke:'#64748b',fill:'rgba(100,116,139,.12)'};return galaxyOriginalRelationTheme(type)};
relationShortLabel=function(type){if(type==='unresolved_path')return'链路待解析';return galaxyOriginalRelationShortLabel(type)};
relationEdgeLabel=function(edge){if(edge&&edge.pending)return'链路待解析';return galaxyOriginalRelationEdgeLabel(edge)};
graphNodes=function(){return(data?.entities||[]).filter(node=>node.type==='account'||node.type==='ib_user')};
function galaxyMergeSnapshots(previous,next){if(!previous)return next;const entityMap=new Map((previous.entities||[]).map(item=>[item.id,item]));for(const item of next.entities||[]){const old=entityMap.get(item.id)||{};entityMap.set(item.id,{...old,...item,scoreLedger:(item.scoreLedger&&item.scoreLedger.length)?item.scoreLedger:(old.scoreLedger||[])});}const edgeMap=new Map();for(const edge of [...(previous.relationships||[]),...(next.relationships||[])]){const key=edge.id||[edge.type,edge.source,edge.target,edge.label].join('|');edgeMap.set(key,{...(edgeMap.get(key)||{}),...edge})}const entities=[...entityMap.values()],relationships=[...edgeMap.values()];const topologyKey=[...entities.filter(item=>item.type==='account'||item.type==='ib_user').map(item=>item.id),...relationships.map(edge=>[edge.type,edge.source,edge.target].join('|'))].sort().join('||');return{...previous,...next,entities,relationships,relationTypes:(next.relationTypes&&next.relationTypes.length)?next.relationTypes:(previous.relationTypes||[]),subjectId:next.subjectId||previous.subjectId,__galaxyTopologyKey:topologyKey};}
const galaxyOriginalRootChainEdges=rootChainEdges;
rootChainEdges=function(nodes){const subject=accounts().find(item=>item.isSubject),out=[],seen=new Set();if(!subject)return out;for(const node of nodes){if(node.id===subject.id)continue;const chain=route(node);if(chain[0]?.id===subject.id){for(let index=1;index<chain.length;index++){const from=chain[index-1],to=chain[index],type=branchType(to),key=from.id+'|'+to.id+'|'+type;if(seen.has(key))continue;seen.add(key);out.push({id:'root|'+key,from,to,type,directed:isDirectedRelation(type)})}continue}const ledger=primary(node),from=ledger&&byId().get(ledger.from),parentNode=from&&(from.type==='account'||from.type==='ib_user')?from:subject,key='pending|'+parentNode.id+'|'+node.id;if(seen.has(key))continue;seen.add(key);out.push({id:key,from:parentNode,to:node,type:'unresolved_path',pending:true,directed:false})}return out};
load=async function(isPoll=false){if(controller)return;if(!target){status.textContent='请从账户详情页进入关系网络，或在地址中提供 account 参数。';return}controller=new AbortController();const active=controller,value=Math.max(1,Math.min(100,Number(threshold.value)||20));threshold.value=value;if(!isPoll)status.textContent=includeToxic.checked?'后台扩散与 Toxic 同向/反向开平仓时间匹配准备中…':'关系后台扩散准备中…';const timeout=setTimeout(()=>active.abort(),8000);try{const response=await fetch(apiUrl(value),{cache:'no-store',signal:active.signal});if(!response.ok)throw new Error();const next=await response.json();if(!next.ok){status.textContent=(next.limitations||[])[0]||'图谱读取失败';return}const nextNodes=(next.entities||[]).filter(item=>item.type==='account'||item.type==='ib_user');if(nextNodes.length||!data)data=galaxyMergeSnapshots(data,next);if(!data){status.textContent='关系后台扩散中：等待首批证据…';queuePoll();return}const hasSelected=(data.entities||[]).some(item=>item.id===selectedId);if(!hasSelected){selectedId=data.subjectId||(data.entities||[]).find(item=>item.isSubject)?.id||'';activeType='';view={scale:1,x:0,y:0}}fit();renderDetail();const progress=data.progress||{},expanded=Number(progress.expandedAccounts||data.summary?.discoveryAccountCount||0),pending=Number(progress.pendingAccounts||data.summary?.pendingAccountCount||0),elapsed=Number(progress.elapsedSeconds||0);if(data.inProgress){status.textContent='后台扩散中：已处理 '+expanded+' 个账户，待处理 '+pending+' 个，已耗时 '+elapsed.toFixed(1)+' 秒；页面可继续使用。';queuePoll()}else status.textContent='规则 '+(data.summary?.ruleVersion||'-')+(data.truncated?' · 已触发安全节点/边上限':' · 已按阈值完成扩散')}catch(error){status.textContent=error.name==='AbortError'?'图谱请求超时，后台任务仍会继续；正在重试。':'图谱读取失败';queuePoll()}finally{clearTimeout(timeout);if(controller===active)controller=null}};
// Collapse all root-to-members edges into one relation-group edge until that group is explicitly expanded.
const galaxyUnaggregatedRootChainEdges=rootChainEdges;
rootChainEdges=function(nodes){const base=galaxyUnaggregatedRootChainEdges(nodes).filter(edge=>!edge.pending),buckets=new Map(),out=[];for(const edge of base){const key=edgeCommunityKey(edge);if(edgeGroupIsExpanded(edge,edge.from?.id)){out.push(edge);continue}if(!buckets.has(key))buckets.set(key,[]);buckets.get(key).push(edge)}for(const [key,items] of buckets){const first=items[0];out.push({...first,id:'root-group|'+key,grouped:true,groupCount:items.length,groupMembers:items.map(item=>item.to)})}return out};
const galaxyRelationEdgeLabel=relationEdgeLabel;
relationEdgeLabel=function(edge){if(edge?.grouped)return relationShortLabel(edge.type)+'（'+Number(edge.groupCount||0)+'个节点）';return galaxyRelationEdgeLabel(edge)};
function galaxyEdgePairKey(edge){const from=String(edge?.from?.id||''),to=String(edge?.to?.id||''),type=edge?.type||'unknown';if(isDirectedRelation(type))return from+'|'+to+'|'+type;return [from,to].sort().join('|')+'|'+type}
const galaxySelectedBranchBeforePairDedup=selectedBranch;
selectedBranch=function(node){const result=galaxySelectedBranchBeforePairDedup(node),rootKeys=new Set(rootChainEdges(graphNodes()).map(galaxyEdgePairKey));return {...result,edges:(result.edges||[]).filter(edge=>edgeIsPath(edge)||!rootKeys.has(galaxyEdgePairKey(edge)))} };
// Ensure every collapsed multi-member community has a visible indirect link back to the
// subject. A community may have members in a late ring whose individual route is not a
// drawable edge after aggregation; use its actual parent (or the subject) as the single
// community-to-community bridge. This is presentation-only and never changes evidence.
// Keep expanded bands visible for collapse, but remove them only from the band's
// hit-test layout. Member nodes remain selectable and keep their route behavior.
const galaxyRingLayoutWithExpandedHitGuard=ringLayout;
let expandedVisibleBands=[],expandedBandCenter=null;
ringLayout=function(all,w,h){const result=galaxyRingLayoutWithExpandedHitGuard(all,w,h);expandedVisibleBands=[];expandedBandCenter=result?.center||null;if(result?.groupsByDepth)for(const depth of [1,2,3,4])for(const group of result.groupsByDepth.get(depth)||[])if(expandedRelationGroups.has(group.key)&&group.nodes.length>1)expandedVisibleBands.push({...group});return result};
const galaxyRootChainWithCommunityBridges=rootChainEdges;
rootChainEdges=function(nodes){
  const base=galaxyRootChainWithCommunityBridges(nodes).filter(edge=>!edge.pending),
        subject=accounts().find(item=>item.isSubject),
        groups=(collapsedVisualGroups||[]).filter(group=>group&&group.nodes?.length>1&&!expandedRelationGroups.has(group.key)),
        memberGroup=new Map(),
        extra=[],seen=new Set(base.map(edge=>galaxyEdgePairKey(edge)+'|'+relationKey(edge.type)));
  if(!subject)return base;
  for(const group of groups)for(const member of group.nodes)memberGroup.set(member.id,group);
  const visibleNode=node=>{
    const group=node&&memberGroup.get(node.id);
    return group?.nodes?.[0]||node;
  };
  const addBridge=(from,to,type,group,segment)=>{
    const source=visibleNode(from),target=visibleNode(to);
    if(!source||!target||source.id===target.id)return;
    const key=galaxyEdgePairKey({from:source,to:target})+'|'+relationKey(type);
    if(seen.has(key))return;
    seen.add(key);
    extra.push({id:'community-bridge|'+group.key+'|'+segment+'|'+source.id+'|'+target.id,from:source,to:target,type,directed:isDirectedRelation(type),grouped:true,groupCount:group.nodes.length,communityBridge:true});
  };
  for(const group of groups){
    const representative=group.nodes[0],owner=group.owner||subject;
    if(!representative||!owner)continue;
    const ownerPath=route(owner);
    if(ownerPath[0]?.id===subject.id){
      for(let index=1;index<ownerPath.length;index++)addBridge(ownerPath[index-1],ownerPath[index],branchType(ownerPath[index]),group,'path'+index);
      addBridge(owner,representative,group.type,group,'members');
    }else{
      // Keep an explicit audit connector when a presentation group has no complete
      // score-ledger route. This is a visual limitation marker, never an inferred fact.
      addBridge(subject,representative,'unresolved_path',group,'unresolved');
    }
  }
  return [...base,...extra];
};
// Explicit, low-misclick controls for group expand/collapse and view reset.
const galaxyControlStyle=document.createElement('style');galaxyControlStyle.textContent='.tools #galaxyResetView{background:#334155;border-color:#64748b}.galaxy-group-actions{margin:8px 0;padding:8px 10px;border:1px solid #24496e;border-radius:8px;background:#08182b}.galaxy-group-actions h3{margin:0 0 6px;font-size:13px;color:#bfdbfe}.galaxy-group-actions-body{display:flex;flex-wrap:wrap;gap:5px;max-height:76px;overflow:auto}.galaxy-group-action{display:inline-flex;align-items:center;gap:6px;padding:4px 7px;border:1px solid #315b80;border-radius:6px;background:#102942;color:#dbeafe;font-size:11px}.galaxy-group-action button{padding:2px 6px;border:1px solid #60a5fa;border-radius:4px;background:#173b5f;color:#e0f2fe;cursor:pointer}.galaxy-group-action button:hover{background:#1d5b8d}.galaxy-group-empty{font-size:11px;color:#94a3b8}';document.head.append(galaxyControlStyle);
const galaxyTools=document.querySelector('.tools'),galaxyResetButton=document.createElement('button');galaxyResetButton.id='galaxyResetView';galaxyResetButton.type='button';galaxyResetButton.className='secondary';galaxyResetButton.textContent='恢复初始';galaxyResetButton.title='仅点击此按钮才恢复初始关系视图';galaxyTools?.append(galaxyResetButton);
const galaxyGroupActions=document.createElement('div');galaxyGroupActions.className='galaxy-group-actions';galaxyGroupActions.innerHTML='<h3>关系群组（显式操作）</h3><div class="galaxy-group-actions-body"><span class="galaxy-group-empty">加载后显示可展开/合并的关系群组</span></div>';document.querySelector('.detail')?.prepend(galaxyGroupActions);
function renderGalaxyGroupActions(){
  const body=galaxyGroupActions.querySelector('.galaxy-group-actions-body');if(!body)return;body.replaceChildren();
  const map=new Map();for(const group of [...(typeof collapsedVisualGroups!=='undefined'?collapsedVisualGroups||[]:[]),...(typeof expandedVisibleBands!=='undefined'?expandedVisibleBands||[]:[])])if(group?.key&&!map.has(group.key))map.set(group.key,group);
  if(!map.size){const empty=document.createElement('span');empty.className='galaxy-group-empty';empty.textContent='当前没有可操作的关系群组';body.append(empty);return}
  for(const group of map.values()){const row=document.createElement('span');row.className='galaxy-group-action';const label=document.createElement('span');label.textContent=(typeof relationShortLabel==='function'?relationShortLabel(group.type):group.type)+' · '+(group.nodes?.length||0)+'个节点';const button=document.createElement('button');const expanded=expandedRelationGroups.has(group.key);button.type='button';button.textContent=expanded?'合并':'展开';button.title=expanded?'合并该关系群组':'展开该关系群组';button.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();if(expandedRelationGroups.has(group.key))expandedRelationGroups.delete(group.key);else expandedRelationGroups.add(group.key);activeGroupKey=group.key;renderOverview();renderDetail()});row.append(label,button);body.append(row)}
}
galaxyResetButton.addEventListener('click',event=>{event.preventDefault();event.stopPropagation();if(!data)return;const subject=accounts().find(item=>item.isSubject);expandedRelationGroups.clear();selectedId=subject?.id||'';activeType='';activeGroupKey='';selectedEdgeKey='';selectedEdgeNodes=new Set();view={scale:1,x:0,y:0};fit();renderOverview();renderDetail();status.textContent='已恢复初始关系视图'});
galaxyGroupActions.remove();
function galaxyExpandedMarkerPoint(group){const center=expandedBandCenter||{x:canvas.width/2,y:canvas.height/2};const anchor=groupAnchorPoint(group,center),dx=anchor.x-center.x,dy=anchor.y-center.y,length=Math.hypot(dx,dy)||1;return{x:anchor.x+dx/length*24,y:anchor.y+dy/length*24}}
function drawGalaxyExpandedMarkers(){if(!data||typeof expandedVisibleBands==='undefined'||!expandedVisibleBands.length)return;ctx.save();ctx.translate(view.x,view.y);ctx.scale(view.scale,view.scale);for(const group of expandedVisibleBands){const marker=galaxyExpandedMarkerPoint(group),theme=relationTheme(group.type);ctx.beginPath();ctx.arc(marker.x,marker.y,13,0,Math.PI*2);ctx.fillStyle='rgba(8,20,36,.97)';ctx.fill();ctx.strokeStyle=theme.stroke;ctx.lineWidth=2.5;ctx.stroke();ctx.fillStyle=theme.stroke;ctx.font='bold 15px Microsoft YaHei';ctx.textAlign='center';ctx.fillText('−',marker.x,marker.y+5)}ctx.textAlign='start';ctx.restore()}
const galaxyRenderOverviewWithControls=renderOverview;renderOverview=function(){galaxyRenderOverviewWithControls();drawGalaxyExpandedMarkers();renderGalaxyGroupActions();document.querySelector('.galaxy-group-actions')?.remove();galaxyRebuildHitFrame()};
queuePoll=function(delay=2000){clearTimeout(pollTimer);if(document.hidden){const resume=()=>{if(document.hidden)return;document.removeEventListener('visibilitychange',resume);queuePoll(250)};document.addEventListener('visibilitychange',resume);return}pollTimer=setTimeout(()=>load(true),delay)};
window.addEventListener('pagehide',()=>{clearTimeout(pollTimer);controller?.abort()});
</script></body></html>"""
